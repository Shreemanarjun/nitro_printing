# Run with `dart run ffigen --config ffigen.yaml`.
name: NitroPrintingBindings
description: |
  Bindings for `src/nitro_printing.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/nitro_printing_bindings_generated.dart'
headers:
  entry-points:
    - 'src/nitro_printing.h'
  include-directives:
    - 'src/nitro_printing.h'
comments:
  style: any
  length: full
