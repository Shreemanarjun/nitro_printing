// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.7.5
// Generated from: nitro_printing.native.dart
// spec-sha256: 69815b3b61dd7cfb2fd141872f0b07b0de7933e4e25a8c9748563fe923a7b802
// ignore_for_file: no_leading_underscores_for_local_identifiers, prefer_typing_uninitialized_variables, non_constant_identifier_names, unused_element, unused_field, unused_import
/// Native (dart:ffi) implementation of [NitroPrinting]. Web builds never
/// compile this library — the platform shim resolves to the web bridge.
library;

import 'package:nitro/nitro.dart';

import '../../nitro_printing.native.dart';

// --- Struct FFI representations (generated by Nitrogen) ---

final class PreviewResultFfi extends Struct {
  external Pointer<Uint8> bytes;
  @Int64()
  external int length;
}

extension PreviewResultFfiExt on PreviewResultFfi {
  PreviewResult toDart() {
    return PreviewResult(
      bytes: Uint8List.fromList(bytes.asTypedList(length)),
      length: length,
    );
  }

  /// Frees internal fields (like strings) that were allocated on the C heap.
  /// Does NOT free the struct itself.
  ///
  /// [nativeFree] must be the free function matching the allocator that
  /// produced the fields — for native-returned structs, the module's
  /// `_nitroFree` (the exported C-runtime free). package:ffi's malloc.free
  /// is CoTaskMemFree on Windows and corrupts the heap on these pointers.
  void freeFields(void Function(Pointer<NativeType>) nativeFree) {}
}

extension PreviewResultExt on PreviewResult {
  Pointer<PreviewResultFfi> toNative(Arena arena) {
    final ptr = arena<PreviewResultFfi>();
    ptr.ref.bytes = bytes.toPointer(arena);
    ptr.ref.length = length;
    return ptr;
  }
}

// --- Struct Native Proxies (zero-copy, lazy field access) ---

/// Zero-copy proxy for [PreviewResult].
/// Extends [PreviewResult] and overrides every getter to read lazily from
/// native memory — no field is copied at construction time.
/// Because `PreviewResultProxy <: PreviewResult`, a `Stream<PreviewResultProxy>`
/// satisfies `Stream<PreviewResult>` via Dart covariant generics.
/// Native memory is freed via a [NativeFinalizer] backed by the
/// generated C symbol 'nitro_printing_release_PreviewResult'.
/// Ownership: this proxy owns the generated struct shell. Zero-copy
/// field buffers remain owned by the native implementation and must
/// stay valid until this proxy is released by the finalizer or
/// [toDartAndRelease].
final class PreviewResultProxy extends PreviewResult implements Finalizable {
  final Pointer<PreviewResultFfi> _native;

  static NativeFinalizer? _finalizer;
  static void Function(Pointer<Void>)? _releaseFn;

  /// Binds the generated release symbol from [dylib].
  /// Must be called once — typically in the impl class constructor.
  /// Idempotent: subsequent calls are a no-op.
  static void _init(DynamicLibrary dylib) {
    _finalizer ??= NativeFinalizer(
      dylib.lookup<NativeFunction<Void Function(Pointer<Void>)>>(
        'nitro_printing_release_PreviewResult',
      ),
    );
    _releaseFn ??= dylib
        .lookupFunction<
          Void Function(Pointer<Void>),
          void Function(Pointer<Void>)
        >('nitro_printing_release_PreviewResult');
  }

  /// Takes ownership of [native]. Super fields are zeroed and never read;
  /// all getters below are overridden to read from native memory instead.
  /// Do NOT call [malloc.free] on the struct shell after passing it here,
  /// and do not free zero-copy field buffers while the proxy may be read.
  PreviewResultProxy(this._native) : super(bytes: Uint8List(0), length: 0) {
    assert(
      _finalizer != null,
      'PreviewResultProxy._init() was not called. Ensure the Nitro impl class constructor ran before creating proxies.',
    );
    _finalizer!.attach(
      this,
      _native.cast(),
      detach: this,
      externalSize: sizeOf<PreviewResultFfi>(),
    );
  }

  // @override lazy getters — read native memory on demand, zero allocation.
  @override
  Uint8List get bytes => _native.ref.bytes.asTypedList(_native.ref.length);
  @override
  int get length => _native.ref.length;

  /// Eagerly copies all fields to a plain [PreviewResult] value, detaches
  /// the finalizer, and frees native memory immediately.
  /// Use this only when you need an immutable snapshot; for streams
  /// prefer consuming the proxy fields lazily then letting it GC.
  /// Must not be called more than once.
  PreviewResult toDartAndRelease() {
    final v = _native.ref.toDart();
    _finalizer?.detach(this);
    _releaseFn!(_native.cast());
    return v;
  }
}

// --- @HybridRecord binary extensions (generated by Nitrogen) ---

extension PrinterInfoRecordFfiExt on PrinterInfo {
  static PrinterInfo fromNative(Pointer<Uint8> ptr) =>
      PrinterInfoRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension PrinterCapabilitiesRecordFfiExt on PrinterCapabilities {
  static PrinterCapabilities fromNative(Pointer<Uint8> ptr) =>
      PrinterCapabilitiesRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension PrintSettingsRecordFfiExt on PrintSettings {
  static PrintSettings fromNative(Pointer<Uint8> ptr) =>
      PrintSettingsRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension PrintDocumentRecordFfiExt on PrintDocument {
  static PrintDocument fromNative(Pointer<Uint8> ptr) =>
      PrintDocumentRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension PrintJobRecordFfiExt on PrintJob {
  static PrintJob fromNative(Pointer<Uint8> ptr) =>
      PrintJobRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension PrintResultRecordFfiExt on PrintResult {
  static PrintResult fromNative(Pointer<Uint8> ptr) =>
      PrintResultRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension PrintDialogResultRecordFfiExt on PrintDialogResult {
  static PrintDialogResult fromNative(Pointer<Uint8> ptr) =>
      PrintDialogResultRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension PrintJobUpdateRecordFfiExt on PrintJobUpdate {
  static PrintJobUpdate fromNative(Pointer<Uint8> ptr) =>
      PrintJobUpdateRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension PrinterStatusRecordFfiExt on PrinterStatus {
  static PrinterStatus fromNative(Pointer<Uint8> ptr) =>
      PrinterStatusRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension DiscoveredPrinterRecordFfiExt on DiscoveredPrinter {
  static DiscoveredPrinter fromNative(Pointer<Uint8> ptr) =>
      DiscoveredPrinterRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

extension PrinterStatusDetailRecordFfiExt on PrinterStatusDetail {
  static PrinterStatusDetail fromNative(Pointer<Uint8> ptr) =>
      PrinterStatusDetailRecordExt.fromReader(RecordReader.fromNative(ptr));

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

class _NitroPrintingImpl extends NitroPrinting {
  final DynamicLibrary _dylib;
  static final _instances = <String, WeakReference<_NitroPrintingImpl>>{};

  static final _instanceFinalizer =
      Finalizer<
        ({
          int id,
          String key,
          Pointer<NitroErrorFfi> err,
          void Function(int) destroy,
        })
      >((t) {
        try {
          t.destroy(t.id);
          NitroRuntime.releaseLib('nitro_printing');
          if (_instances[t.key]?.target == null) _instances.remove(t.key);
        } catch (_) {
        } finally {
          calloc.free(t.err);
        }
      });
  final String _instanceKey;
  late final int _instanceId;
  final Pointer<NitroErrorFfi> _nitroErr = calloc<NitroErrorFfi>();
  late final void Function(Pointer<Void>) _nitroFreePtr = _dylib
      .lookupFunction<
        Void Function(Pointer<Void>),
        void Function(Pointer<Void>)
      >('nitro_printing_nitro_free');
  void _nitroFree(Pointer<NativeType> ptr) => _nitroFreePtr(ptr.cast());
  late final Pointer<NativeFinalizerFunction> _nitroFreeFinalizer = _dylib
      .lookup<NativeFinalizerFunction>('nitro_printing_nitro_free')
      .cast();
  late final Pointer<Void> Function(int) _nitroAllocPtr = _dylib
      .lookupFunction<
        Pointer<Void> Function(IntPtr),
        Pointer<Void> Function(int)
      >('nitro_printing_nitro_alloc');
  late final NitroNativeAllocator _nitroNativeAllocator = NitroNativeAllocator(
    _nitroAllocPtr,
    _nitroFreePtr,
  );

  static DynamicLibrary _loadSupportedLibrary() {
    assert(
      !const bool.fromEnvironment('dart.library.js_interop'),
      'nitro_printing: dart:ffi is unavailable on web. Instantiate the web bridge via createNitroPrintingWebInstance() from the generated *.web.bridge.g.dart file instead.',
    );
    return NitroRuntime.loadLibForTargets(
      'nitro_printing',
      ios: true,
      android: true,
      macos: true,
      windows: true,
      linux: true,
      web: true,
    );
  }

  factory _NitroPrintingImpl([String key = 'default']) {
    final cached = _instances[key]?.target;
    if (cached != null) return cached;
    return _NitroPrintingImpl._init(key);
  }

  _NitroPrintingImpl._init(this._instanceKey)
    : _dylib = _loadSupportedLibrary() {
    final initSw = Stopwatch()..start();
    assert(
      sizeOf<IntPtr>() >= 4,
      'nitro_printing: unsupported pointer width ${sizeOf<IntPtr>()}B',
    );
    final initFunc = _dylib
        .lookupFunction<
          IntPtr Function(Pointer<Void>),
          int Function(Pointer<Void>)
        >('nitro_printing_init_dart_api_dl');
    final initCode = initFunc(NativeApi.initializeApiDLData);
    if (initCode != 0) {
      throw StateError(
        'nitro_printing: Dart API DL initialization failed with code $initCode.',
      );
    }
    if (!_dylib.providesSymbol('nitro_printing_nitro_abi_version')) {
      throw StateError(
        'nitro_printing: missing nitro_printing_nitro_abi_version — plugin must be rebuilt against the current Nitro version.',
      );
    }
    NitroRuntime.checkAbiVersion(
      'nitro_printing',
      () => _dylib.lookupFunction<Uint32 Function(), int Function()>(
        'nitro_printing_nitro_abi_version',
      )(),
    );
    if (!_dylib.providesSymbol('nitro_printing_nitro_bridge_checksum')) {
      throw StateError(
        'nitro_printing: missing nitro_printing_nitro_bridge_checksum — plugin must be rebuilt against the current Nitro version.',
      );
    }
    NitroRuntime.checkLinkChecksum(
      'nitro_printing',
      '0c4b52ee5bb1ae9e',
      () => _dylib
          .lookupFunction<Pointer<Utf8> Function(), Pointer<Utf8> Function()>(
            'nitro_printing_nitro_bridge_checksum',
          )()
          .toDartString(),
    );
    PreviewResultProxy._init(_dylib);
    final _keyPtr = _instanceKey.toNativeUtf8(allocator: calloc);
    try {
      _instanceId = _createInstancePtr(_keyPtr);
      if (_instanceId < 0) {
        throw StateError(
          'nitro_printing: failed to create native instance for key "$_instanceKey".',
        );
      }
    } finally {
      calloc.free(_keyPtr);
    }
    NitroInstanceRegistry.register(_instanceId, this);
    _instances[_instanceKey] = WeakReference(this);
    _instanceFinalizer.attach(this, (
      id: _instanceId,
      key: _instanceKey,
      err: _nitroErr,
      destroy: _destroyInstancePtr,
    ), detach: this);
    initSw.stop();
    NitroRuntime.logLifecycle(
      'init(nitro_printing)',
      'initialized in ${initSw.elapsedMicroseconds} µs (instanceId=$_instanceId)',
    );
  }

  late final int Function(Pointer<Utf8>) _createInstancePtr = _dylib
      .lookupFunction<
        Int64 Function(Pointer<Utf8>),
        int Function(Pointer<Utf8>)
      >('nitro_printing_create_instance');
  late final void Function(int) _destroyInstancePtr = _dylib
      .lookupFunction<Void Function(Int64), void Function(int)>(
        'nitro_printing_destroy_instance',
      );
  late final bool Function(int, Pointer<NitroErrorFfi>)
  _isPrintingSupportedPtr = _dylib
      .lookup<NativeFunction<Bool Function(Int64, Pointer<NitroErrorFfi>)>>(
        'nitro_printing_is_printing_supported',
      )
      .asFunction<bool Function(int, Pointer<NitroErrorFfi>)>(isLeaf: true);
  late final int Function(int, Pointer<NitroErrorFfi>)
  _getPrintersCountPtr = _dylib
      .lookup<NativeFunction<Int64 Function(Int64, Pointer<NitroErrorFfi>)>>(
        'nitro_printing_get_printers_count',
      )
      .asFunction<int Function(int, Pointer<NitroErrorFfi>)>(isLeaf: true);
  late final Pointer<Utf8> Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>)
  _getPrinterDriverVersionPtr = _dylib
      .lookupFunction<
        Pointer<Utf8> Function(Int64, Pointer<Utf8>, Pointer<NitroErrorFfi>),
        Pointer<Utf8> Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>)
      >('nitro_printing_get_printer_driver_version');
  late final void Function(int, Pointer<NitroErrorFfi>, int)
  _getAllPrintersPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<NitroErrorFfi>, int)
      >('nitro_printing_get_all_printers');
  late final Pointer<Uint8> Function(int, int) _getPrinterAtPtr = _dylib
      .lookupFunction<
        Pointer<Uint8> Function(Int64, Int64),
        Pointer<Uint8> Function(int, int)
      >('nitro_printing_get_printer_at');
  late final Pointer<Uint8> Function(int) _getDefaultPrinterPtr = _dylib
      .lookupFunction<
        Pointer<Uint8> Function(Int64),
        Pointer<Uint8> Function(int)
      >('nitro_printing_get_default_printer');
  late final Pointer<Uint8> Function(int, Pointer<Utf8>)
  _getPrinterCapabilitiesPtr = _dylib
      .lookupFunction<
        Pointer<Uint8> Function(Int64, Pointer<Utf8>),
        Pointer<Uint8> Function(int, Pointer<Utf8>)
      >('nitro_printing_get_printer_capabilities');
  late final void Function(
    int,
    Pointer<Utf8>,
    Pointer<Uint8>,
    Pointer<NitroErrorFfi>,
    int,
  )
  _printTextPtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Utf8>,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Utf8>,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_printing_print_text');
  late final void Function(
    int,
    Pointer<Uint8>,
    int,
    Pointer<Uint8>,
    Pointer<NitroErrorFfi>,
    int,
  )
  _printImagePtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Uint8>,
          Size,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Uint8>,
          int,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_printing_print_image');
  late final void Function(
    int,
    Pointer<Uint8>,
    int,
    Pointer<Uint8>,
    Pointer<NitroErrorFfi>,
    int,
  )
  _printPdfPtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Uint8>,
          Size,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Uint8>,
          int,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_printing_print_pdf');
  late final void Function(
    int,
    Pointer<Uint8>,
    Pointer<Uint8>,
    Pointer<NitroErrorFfi>,
    int,
  )
  _printDocumentPtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Uint8>,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Uint8>,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_printing_print_document');
  late final void Function(
    int,
    Pointer<Utf8>,
    Pointer<Uint8>,
    Pointer<NitroErrorFfi>,
    int,
  )
  _printFilePtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Utf8>,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Utf8>,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_printing_print_file');
  late final void Function(
    int,
    Pointer<Uint8>,
    bool,
    Pointer<Uint8>,
    Pointer<NitroErrorFfi>,
    int,
  )
  _printBatchPtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Uint8>,
          Bool,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Uint8>,
          bool,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_printing_print_batch');
  late final void Function(
    int,
    Pointer<Uint8>,
    Pointer<Uint8>,
    Pointer<NitroErrorFfi>,
    int,
  )
  _showPrintDialogPtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Uint8>,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Uint8>,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_printing_show_print_dialog');
  late final void Function(
    int,
    Pointer<Uint8>,
    Pointer<Uint8>,
    Pointer<NitroErrorFfi>,
    int,
  )
  _renderPreviewPtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Uint8>,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Uint8>,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_printing_render_preview');
  late final void Function(int, Pointer<Uint8>, Pointer<NitroErrorFfi>, int)
  _getPageCountPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<Uint8>, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<Uint8>, Pointer<NitroErrorFfi>, int)
      >('nitro_printing_get_page_count');
  late final void Function(
    int,
    Pointer<Uint8>,
    Pointer<Utf8>,
    Pointer<Uint8>,
    Pointer<NitroErrorFfi>,
    int,
  )
  _printToFilePtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Uint8>,
          Pointer<Utf8>,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Uint8>,
          Pointer<Utf8>,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_printing_print_to_file');
  late final void Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>, int)
  _cancelPrintJobPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<Utf8>, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>, int)
      >('nitro_printing_cancel_print_job');
  late final void Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>, int)
  _pausePrintJobPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<Utf8>, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>, int)
      >('nitro_printing_pause_print_job');
  late final void Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>, int)
  _resumePrintJobPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<Utf8>, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>, int)
      >('nitro_printing_resume_print_job');
  late final void Function(int, Pointer<NitroErrorFfi>, int)
  _clearPrintQueuePtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<NitroErrorFfi>, int)
      >('nitro_printing_clear_print_queue');
  late final void Function(int, Pointer<NitroErrorFfi>, int)
  _getPrintJobsCountPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<NitroErrorFfi>, int)
      >('nitro_printing_get_print_jobs_count');
  late final Pointer<Uint8> Function(int, int) _getPrintJobAtPtr = _dylib
      .lookupFunction<
        Pointer<Uint8> Function(Int64, Int64),
        Pointer<Uint8> Function(int, int)
      >('nitro_printing_get_print_job_at');
  late final Pointer<Uint8> Function(int, Pointer<Utf8>) _getPrintJobStatusPtr =
      _dylib.lookupFunction<
        Pointer<Uint8> Function(Int64, Pointer<Utf8>),
        Pointer<Uint8> Function(int, Pointer<Utf8>)
      >('nitro_printing_get_print_job_status');
  late final void Function(int, Pointer<NitroErrorFfi>, int)
  _startPrinterDiscoveryPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<NitroErrorFfi>, int)
      >('nitro_printing_start_printer_discovery');
  late final void Function(int, Pointer<NitroErrorFfi>, int)
  _stopPrinterDiscoveryPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<NitroErrorFfi>, int)
      >('nitro_printing_stop_printer_discovery');
  late final void Function(
    int,
    Pointer<Utf8>,
    Pointer<NitroOptInt64>,
    Pointer<NitroErrorFfi>,
    int,
  )
  _testPrinterConnectionPtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Utf8>,
          Pointer<NitroOptInt64>,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Utf8>,
          Pointer<NitroOptInt64>,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_printing_test_printer_connection');
  late final void Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>, int)
  _setDefaultPrinterPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<Utf8>, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>, int)
      >('nitro_printing_set_default_printer');
  late final void Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>, int)
  _openSystemPrintQueuePtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<Utf8>, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>, int)
      >('nitro_printing_open_system_print_queue');
  late final void Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>, int)
  _openPrinterPropertiesPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<Utf8>, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>, int)
      >('nitro_printing_open_printer_properties');
  late final void Function(
    int,
    Pointer<Uint8>,
    int,
    Pointer<Uint8>,
    Pointer<NitroErrorFfi>,
    int,
  )
  _printRawPtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Uint8>,
          Size,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Uint8>,
          int,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_printing_print_raw');
  late final void Function(
    int,
    Pointer<Uint8>,
    int,
    Pointer<Uint8>,
    Pointer<NitroErrorFfi>,
    int,
  )
  _printEscPosPtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Uint8>,
          Size,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Uint8>,
          int,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_printing_print_esc_pos');
  late final void Function(
    int,
    Pointer<Utf8>,
    Pointer<Uint8>,
    Pointer<NitroErrorFfi>,
    int,
  )
  _printZplPtr = _dylib
      .lookupFunction<
        Void Function(
          Int64,
          Pointer<Utf8>,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          Int64,
        ),
        void Function(
          int,
          Pointer<Utf8>,
          Pointer<Uint8>,
          Pointer<NitroErrorFfi>,
          int,
        )
      >('nitro_printing_print_zpl');
  late final void Function(int, Pointer<NitroErrorFfi>, int)
  _cancelRawPrintPtr = _dylib
      .lookupFunction<
        Void Function(Int64, Pointer<NitroErrorFfi>, Int64),
        void Function(int, Pointer<NitroErrorFfi>, int)
      >('nitro_printing_cancel_raw_print');
  late final Pointer<Uint8> Function(int, Pointer<Utf8>, Pointer<NitroOptInt64>)
  _getPrinterStatusDetailPtr = _dylib
      .lookupFunction<
        Pointer<Uint8> Function(Int64, Pointer<Utf8>, Pointer<NitroOptInt64>),
        Pointer<Uint8> Function(int, Pointer<Utf8>, Pointer<NitroOptInt64>)
      >('nitro_printing_get_printer_status_detail');
  late final void Function(int, int) _registerOnPrintJobChangedPtr = _dylib
      .lookupFunction<Void Function(Int64, Int64), void Function(int, int)>(
        'nitro_printing_register_on_print_job_changed_stream',
      );
  late final void Function(int) _releaseOnPrintJobChangedPtr = _dylib
      .lookupFunction<Void Function(Int64), void Function(int)>(
        'nitro_printing_release_on_print_job_changed_stream',
      );
  late final void Function(int, int) _registerOnPrinterStatusChangedPtr = _dylib
      .lookupFunction<Void Function(Int64, Int64), void Function(int, int)>(
        'nitro_printing_register_on_printer_status_changed_stream',
      );
  late final void Function(int) _releaseOnPrinterStatusChangedPtr = _dylib
      .lookupFunction<Void Function(Int64), void Function(int)>(
        'nitro_printing_release_on_printer_status_changed_stream',
      );
  late final void Function(int, int) _registerOnPrinterDiscoveredPtr = _dylib
      .lookupFunction<Void Function(Int64, Int64), void Function(int, int)>(
        'nitro_printing_register_on_printer_discovered_stream',
      );
  late final void Function(int) _releaseOnPrinterDiscoveredPtr = _dylib
      .lookupFunction<Void Function(Int64), void Function(int)>(
        'nitro_printing_release_on_printer_discovered_stream',
      );
  // ignore: unused_field
  late final Pointer<NitroErrorFfi> Function() _getErrorPtr = _dylib
      .lookupFunction<
        Pointer<NitroErrorFfi> Function(),
        Pointer<NitroErrorFfi> Function()
      >('nitro_printing_get_error');
  // ignore: unused_field
  late final void Function() _clearErrorPtr = _dylib
      .lookupFunction<Void Function(), void Function()>(
        'nitro_printing_clear_error',
      );
  // ignore: unused_field
  late final Pointer<NativeFunction<Pointer<NitroErrorFfi> Function()>>
  _getErrorNativePtr = _dylib.lookup('nitro_printing_get_error');
  // ignore: unused_field
  late final Pointer<NativeFunction<Void Function()>> _clearErrorNativePtr =
      _dylib.lookup('nitro_printing_clear_error');

  @override
  void dispose() {
    if (isDisposed) return;
    _instanceFinalizer.detach(this);
    NitroRuntime.logLifecycle(
      'dispose(nitro_printing)',
      'disposing (instanceId=$_instanceId)',
    );
    _destroyInstancePtr(_instanceId);
    NitroRuntime.releaseLib('nitro_printing');
    _instances.remove(_instanceKey);
    NitroInstanceRegistry.unregister(_instanceId, this);
    calloc.free(_nitroErr); // S8: free pre-allocated error slot
    super.dispose(); // sets isDisposed = true, calls onDestroy()
    NitroRuntime.logLifecycle('dispose(nitro_printing)', 'disposed');
  }

  /// Opaque reference to this native impl — pass to other plugins via AnyNativeObject.
  AnyNativeObject get asAnyNativeObject => AnyNativeObject(_instanceId);

  @override
  bool isPrintingSupported() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final res = _isPrintingSupportedPtr(_instanceId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      return res;
    }, methodName: 'isPrintingSupported');
  }

  @override
  int getPrintersCount() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final res = _getPrintersCountPtr(_instanceId, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
      return res;
    }, methodName: 'getPrintersCount');
  }

  @override
  String getPrinterDriverVersion(String printerId) {
    checkDisposed();
    return NitroRuntime.callSync(
      () => withArena((arena) {
        final res = _getPrinterDriverVersionPtr(
          _instanceId,
          printerId.toNativeUtf8(allocator: arena),
          _nitroErr,
        );
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
        return res.toDartStringBorrowed();
      }),
      methodName: 'getPrinterDriverVersion',
    );
  }

  @override
  Future<List<PrinterInfo>> getAllPrinters() {
    checkDisposed();
    final _nitroErr = calloc<NitroErrorFfi>();
    return NitroRuntime.openNativeAsync<List<PrinterInfo>>(
      call: (port) => _getAllPrintersPtr(_instanceId, _nitroErr, port),
      unpack: (raw) {
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
        return ((raw) {
          if (raw == null) {
            throw StateError(
              "getAllPrinters (native-async): native posted null for a non-nullable List<PrinterInfo> result — post the encoded value, or make the return type nullable",
            );
          }
          final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
          return LazyRecordList.decode(
            rawPtr,
            (r) => PrinterInfoRecordExt.fromReader(r),
            nativeFree: _nitroFreeFinalizer,
          );
        })(raw);
      },
      cleanup: () => calloc.free(_nitroErr),
      methodName: 'getAllPrinters',
    );
  }

  @override
  Future<NitroResultValue<PrinterInfo>> getPrinterAt(int index) async {
    checkDisposed();
    final res = await NitroRuntime.callAsync<Pointer<Uint8>>(
      _getPrinterAtPtr,
      [_instanceId, index],
      getError: _getErrorNativePtr,
      clearError: _clearErrorNativePtr,
      methodName: 'getPrinterAt',
    );
    try {
      final _tag = res[0];
      if (_tag != 0) {
        final _errR = RecordReader.fromNative(res + 1);
        return NitroErr(_errR.readString());
      }
      return NitroOk(PrinterInfoRecordFfiExt.fromNative(res + 1));
    } finally {
      _nitroFree(res);
    }
  }

  @override
  Future<NitroResultValue<PrinterInfo>> getDefaultPrinter() async {
    checkDisposed();
    final res = await NitroRuntime.callAsync<Pointer<Uint8>>(
      _getDefaultPrinterPtr,
      [_instanceId],
      getError: _getErrorNativePtr,
      clearError: _clearErrorNativePtr,
      methodName: 'getDefaultPrinter',
    );
    try {
      final _tag = res[0];
      if (_tag != 0) {
        final _errR = RecordReader.fromNative(res + 1);
        return NitroErr(_errR.readString());
      }
      return NitroOk(PrinterInfoRecordFfiExt.fromNative(res + 1));
    } finally {
      _nitroFree(res);
    }
  }

  @override
  Future<NitroResultValue<PrinterCapabilities>> getPrinterCapabilities(
    String printerId,
  ) async {
    checkDisposed();
    final arena = Arena();
    try {
      final res = await NitroRuntime.callAsync<Pointer<Uint8>>(
        _getPrinterCapabilitiesPtr,
        [_instanceId, printerId.toNativeUtf8(allocator: arena)],
        getError: _getErrorNativePtr,
        clearError: _clearErrorNativePtr,
        methodName: 'getPrinterCapabilities',
      );
      try {
        final _tag = res[0];
        if (_tag != 0) {
          final _errR = RecordReader.fromNative(res + 1);
          return NitroErr(_errR.readString());
        }
        return NitroOk(PrinterCapabilitiesRecordFfiExt.fromNative(res + 1));
      } finally {
        _nitroFree(res);
      }
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<PrintResult> printText(String text, {PrintSettings? settings}) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<PrintResult>(
        call: (port) => _printTextPtr(
          _instanceId,
          text.toNativeUtf8(allocator: arena),
          settings != null ? settings.toNative(arena) : nullptr,
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) {
            if (raw == null) {
              throw StateError(
                "printText (native-async): native posted null for a non-nullable PrintResult result — post the encoded value, or make the return type nullable",
              );
            }
            final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
            try {
              return PrintResultRecordFfiExt.fromNative(rawPtr);
            } finally {
              _nitroFree(rawPtr);
            }
          })(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'printText',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<PrintResult> printImage(
    Uint8List imageData, {
    PrintSettings? settings,
  }) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<PrintResult>(
        call: (port) => _printImagePtr(
          _instanceId,
          imageData.toPointer(arena),
          imageData.length,
          settings != null ? settings.toNative(arena) : nullptr,
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) {
            if (raw == null) {
              throw StateError(
                "printImage (native-async): native posted null for a non-nullable PrintResult result — post the encoded value, or make the return type nullable",
              );
            }
            final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
            try {
              return PrintResultRecordFfiExt.fromNative(rawPtr);
            } finally {
              _nitroFree(rawPtr);
            }
          })(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'printImage',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<PrintResult> printPdf(Uint8List pdfData, {PrintSettings? settings}) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<PrintResult>(
        call: (port) => _printPdfPtr(
          _instanceId,
          pdfData.toPointer(arena),
          pdfData.length,
          settings != null ? settings.toNative(arena) : nullptr,
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) {
            if (raw == null) {
              throw StateError(
                "printPdf (native-async): native posted null for a non-nullable PrintResult result — post the encoded value, or make the return type nullable",
              );
            }
            final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
            try {
              return PrintResultRecordFfiExt.fromNative(rawPtr);
            } finally {
              _nitroFree(rawPtr);
            }
          })(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'printPdf',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<PrintResult> printDocument(
    PrintDocument document, {
    PrintSettings? settings,
  }) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<PrintResult>(
        call: (port) => _printDocumentPtr(
          _instanceId,
          document.toNative(arena),
          settings != null ? settings.toNative(arena) : nullptr,
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) {
            if (raw == null) {
              throw StateError(
                "printDocument (native-async): native posted null for a non-nullable PrintResult result — post the encoded value, or make the return type nullable",
              );
            }
            final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
            try {
              return PrintResultRecordFfiExt.fromNative(rawPtr);
            } finally {
              _nitroFree(rawPtr);
            }
          })(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'printDocument',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<bool> printFile(String filePath, {PrintSettings? settings}) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<bool>(
        call: (port) => _printFilePtr(
          _instanceId,
          filePath.toNativeUtf8(allocator: arena),
          settings != null ? settings.toNative(arena) : nullptr,
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) => raw as bool)(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'printFile',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<List<PrintResult>> printBatch(
    List<PrintDocument> documents,
    bool stopOnError, {
    PrintSettings? settings,
  }) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<List<PrintResult>>(
        call: (port) => _printBatchPtr(
          _instanceId,
          RecordWriter.encodeIndexedList(
            documents,
            (w, e) => e.writeFields(w),
            arena,
          ),
          stopOnError,
          settings != null ? settings.toNative(arena) : nullptr,
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) {
            if (raw == null) {
              throw StateError(
                "printBatch (native-async): native posted null for a non-nullable List<PrintResult> result — post the encoded value, or make the return type nullable",
              );
            }
            final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
            return LazyRecordList.decode(
              rawPtr,
              (r) => PrintResultRecordExt.fromReader(r),
              nativeFree: _nitroFreeFinalizer,
            );
          })(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'printBatch',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<PrintDialogResult> showPrintDialog(
    PrintDocument document, {
    PrintSettings? initialSettings,
  }) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<PrintDialogResult>(
        call: (port) => _showPrintDialogPtr(
          _instanceId,
          document.toNative(arena),
          initialSettings != null ? initialSettings.toNative(arena) : nullptr,
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) {
            if (raw == null) {
              throw StateError(
                "showPrintDialog (native-async): native posted null for a non-nullable PrintDialogResult result — post the encoded value, or make the return type nullable",
              );
            }
            final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
            try {
              return PrintDialogResultRecordFfiExt.fromNative(rawPtr);
            } finally {
              _nitroFree(rawPtr);
            }
          })(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'showPrintDialog',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<PreviewResult> renderPreview(
    PrintDocument document, {
    PrintSettings? settings,
  }) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<PreviewResult>(
        call: (port) => _renderPreviewPtr(
          _instanceId,
          document.toNative(arena),
          settings != null ? settings.toNative(arena) : nullptr,
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) {
            if (raw == null) {
              throw StateError(
                "renderPreview (native-async): native posted null for a non-nullable PreviewResult result — post the encoded value, or make the return type nullable",
              );
            }
            final ptr = Pointer<PreviewResultFfi>.fromAddress(raw as int);
            try {
              return ptr.ref.toDart();
            } finally {
              ptr.ref.freeFields(_nitroFree);
              _nitroFree(ptr);
            }
          })(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'renderPreview',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<int> getPageCount(PrintDocument document) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<int>(
        call: (port) => _getPageCountPtr(
          _instanceId,
          document.toNative(arena),
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) => raw as int)(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'getPageCount',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<bool> printToFile(
    PrintDocument document,
    String outputPath, {
    PrintSettings? settings,
  }) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<bool>(
        call: (port) => _printToFilePtr(
          _instanceId,
          document.toNative(arena),
          outputPath.toNativeUtf8(allocator: arena),
          settings != null ? settings.toNative(arena) : nullptr,
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) => raw as bool)(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'printToFile',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<bool> cancelPrintJob(String jobId) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<bool>(
        call: (port) => _cancelPrintJobPtr(
          _instanceId,
          jobId.toNativeUtf8(allocator: arena),
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) => raw as bool)(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'cancelPrintJob',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<bool> pausePrintJob(String jobId) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<bool>(
        call: (port) => _pausePrintJobPtr(
          _instanceId,
          jobId.toNativeUtf8(allocator: arena),
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) => raw as bool)(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'pausePrintJob',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<bool> resumePrintJob(String jobId) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<bool>(
        call: (port) => _resumePrintJobPtr(
          _instanceId,
          jobId.toNativeUtf8(allocator: arena),
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) => raw as bool)(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'resumePrintJob',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<bool> clearPrintQueue() {
    checkDisposed();
    final _nitroErr = calloc<NitroErrorFfi>();
    return NitroRuntime.openNativeAsync<bool>(
      call: (port) => _clearPrintQueuePtr(_instanceId, _nitroErr, port),
      unpack: (raw) {
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
        return ((raw) => raw as bool)(raw);
      },
      cleanup: () => calloc.free(_nitroErr),
      methodName: 'clearPrintQueue',
    );
  }

  @override
  Future<int> getPrintJobsCount() {
    checkDisposed();
    final _nitroErr = calloc<NitroErrorFfi>();
    return NitroRuntime.openNativeAsync<int>(
      call: (port) => _getPrintJobsCountPtr(_instanceId, _nitroErr, port),
      unpack: (raw) {
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
        return ((raw) => raw as int)(raw);
      },
      cleanup: () => calloc.free(_nitroErr),
      methodName: 'getPrintJobsCount',
    );
  }

  @override
  Future<NitroResultValue<PrintJob>> getPrintJobAt(int index) async {
    checkDisposed();
    final res = await NitroRuntime.callAsync<Pointer<Uint8>>(
      _getPrintJobAtPtr,
      [_instanceId, index],
      getError: _getErrorNativePtr,
      clearError: _clearErrorNativePtr,
      methodName: 'getPrintJobAt',
    );
    try {
      final _tag = res[0];
      if (_tag != 0) {
        final _errR = RecordReader.fromNative(res + 1);
        return NitroErr(_errR.readString());
      }
      return NitroOk(PrintJobRecordFfiExt.fromNative(res + 1));
    } finally {
      _nitroFree(res);
    }
  }

  @override
  Future<NitroResultValue<PrintJob>> getPrintJobStatus(String jobId) async {
    checkDisposed();
    final arena = Arena();
    try {
      final res = await NitroRuntime.callAsync<Pointer<Uint8>>(
        _getPrintJobStatusPtr,
        [_instanceId, jobId.toNativeUtf8(allocator: arena)],
        getError: _getErrorNativePtr,
        clearError: _clearErrorNativePtr,
        methodName: 'getPrintJobStatus',
      );
      try {
        final _tag = res[0];
        if (_tag != 0) {
          final _errR = RecordReader.fromNative(res + 1);
          return NitroErr(_errR.readString());
        }
        return NitroOk(PrintJobRecordFfiExt.fromNative(res + 1));
      } finally {
        _nitroFree(res);
      }
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<bool> startPrinterDiscovery() {
    checkDisposed();
    final _nitroErr = calloc<NitroErrorFfi>();
    return NitroRuntime.openNativeAsync<bool>(
      call: (port) => _startPrinterDiscoveryPtr(_instanceId, _nitroErr, port),
      unpack: (raw) {
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
        return ((raw) => raw as bool)(raw);
      },
      cleanup: () => calloc.free(_nitroErr),
      methodName: 'startPrinterDiscovery',
    );
  }

  @override
  Future<bool> stopPrinterDiscovery() {
    checkDisposed();
    final _nitroErr = calloc<NitroErrorFfi>();
    return NitroRuntime.openNativeAsync<bool>(
      call: (port) => _stopPrinterDiscoveryPtr(_instanceId, _nitroErr, port),
      unpack: (raw) {
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
        return ((raw) => raw as bool)(raw);
      },
      cleanup: () => calloc.free(_nitroErr),
      methodName: 'stopPrinterDiscovery',
    );
  }

  @override
  Future<bool> testPrinterConnection(String printerId, {int? timeoutSeconds}) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<bool>(
        call: (port) => _testPrinterConnectionPtr(
          _instanceId,
          printerId.toNativeUtf8(allocator: arena),
          arena.packInt(timeoutSeconds),
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) => raw as bool)(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'testPrinterConnection',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<bool> setDefaultPrinter(String printerId) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<bool>(
        call: (port) => _setDefaultPrinterPtr(
          _instanceId,
          printerId.toNativeUtf8(allocator: arena),
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) => raw as bool)(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'setDefaultPrinter',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<bool> openSystemPrintQueue(String printerId) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<bool>(
        call: (port) => _openSystemPrintQueuePtr(
          _instanceId,
          printerId.toNativeUtf8(allocator: arena),
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) => raw as bool)(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'openSystemPrintQueue',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<bool> openPrinterProperties(String printerId) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<bool>(
        call: (port) => _openPrinterPropertiesPtr(
          _instanceId,
          printerId.toNativeUtf8(allocator: arena),
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) => raw as bool)(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'openPrinterProperties',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<PrintResult> printRaw(Uint8List data, {PrintSettings? settings}) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<PrintResult>(
        call: (port) => _printRawPtr(
          _instanceId,
          data.toPointer(arena),
          data.length,
          settings != null ? settings.toNative(arena) : nullptr,
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) {
            if (raw == null) {
              throw StateError(
                "printRaw (native-async): native posted null for a non-nullable PrintResult result — post the encoded value, or make the return type nullable",
              );
            }
            final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
            try {
              return PrintResultRecordFfiExt.fromNative(rawPtr);
            } finally {
              _nitroFree(rawPtr);
            }
          })(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'printRaw',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<PrintResult> printEscPos(
    Uint8List escPosData, {
    PrintSettings? settings,
  }) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<PrintResult>(
        call: (port) => _printEscPosPtr(
          _instanceId,
          escPosData.toPointer(arena),
          escPosData.length,
          settings != null ? settings.toNative(arena) : nullptr,
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) {
            if (raw == null) {
              throw StateError(
                "printEscPos (native-async): native posted null for a non-nullable PrintResult result — post the encoded value, or make the return type nullable",
              );
            }
            final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
            try {
              return PrintResultRecordFfiExt.fromNative(rawPtr);
            } finally {
              _nitroFree(rawPtr);
            }
          })(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'printEscPos',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<PrintResult> printZpl(String zpl, {PrintSettings? settings}) {
    checkDisposed();
    final arena = Arena();
    final _nitroErr = calloc<NitroErrorFfi>();
    try {
      return NitroRuntime.openNativeAsync<PrintResult>(
        call: (port) => _printZplPtr(
          _instanceId,
          zpl.toNativeUtf8(allocator: arena),
          settings != null ? settings.toNative(arena) : nullptr,
          _nitroErr,
          port,
        ),
        unpack: (raw) {
          NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
          return ((raw) {
            if (raw == null) {
              throw StateError(
                "printZpl (native-async): native posted null for a non-nullable PrintResult result — post the encoded value, or make the return type nullable",
              );
            }
            final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
            try {
              return PrintResultRecordFfiExt.fromNative(rawPtr);
            } finally {
              _nitroFree(rawPtr);
            }
          })(raw);
        },
        cleanup: () => calloc.free(_nitroErr),
        methodName: 'printZpl',
      );
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Future<bool> cancelRawPrint() {
    checkDisposed();
    final _nitroErr = calloc<NitroErrorFfi>();
    return NitroRuntime.openNativeAsync<bool>(
      call: (port) => _cancelRawPrintPtr(_instanceId, _nitroErr, port),
      unpack: (raw) {
        NitroRuntime.throwIfOutParamError(_nitroErr, nativeFree: _nitroFree);
        return ((raw) => raw as bool)(raw);
      },
      cleanup: () => calloc.free(_nitroErr),
      methodName: 'cancelRawPrint',
    );
  }

  @override
  Future<NitroResultValue<PrinterStatusDetail>> getPrinterStatusDetail(
    String printerId, {
    int? timeoutSeconds,
  }) async {
    checkDisposed();
    final arena = Arena();
    try {
      final res = await NitroRuntime.callAsync<Pointer<Uint8>>(
        _getPrinterStatusDetailPtr,
        [
          _instanceId,
          printerId.toNativeUtf8(allocator: arena),
          arena.packInt(timeoutSeconds),
        ],
        getError: _getErrorNativePtr,
        clearError: _clearErrorNativePtr,
        methodName: 'getPrinterStatusDetail',
      );
      try {
        final _tag = res[0];
        if (_tag != 0) {
          final _errR = RecordReader.fromNative(res + 1);
          return NitroErr(_errR.readString());
        }
        return NitroOk(PrinterStatusDetailRecordFfiExt.fromNative(res + 1));
      } finally {
        _nitroFree(res);
      }
    } finally {
      arena.releaseAll();
    }
  }

  @override
  Stream<PrintJobUpdate> onPrintJobChanged() {
    checkDisposed();
    return NitroRuntime.openStream<PrintJobUpdate>(
      register: (port) => _registerOnPrintJobChangedPtr(_instanceId, port),
      unpack: (message) {
        if (message == null) {
          throw StateError(
            'Received null event on non-nullable stream onPrintJobChanged',
          );
        }
        final rawPtr = Pointer<Uint8>.fromAddress(message as int);
        try {
          return PrintJobUpdateRecordFfiExt.fromNative(rawPtr);
        } finally {
          _nitroFree(rawPtr);
        }
      },
      release: (port) => _releaseOnPrintJobChangedPtr(port),
      backpressure: Backpressure.dropLatest,
    );
  }

  @override
  Stream<PrinterStatus> onPrinterStatusChanged() {
    checkDisposed();
    return NitroRuntime.openStream<PrinterStatus>(
      register: (port) => _registerOnPrinterStatusChangedPtr(_instanceId, port),
      unpack: (message) {
        if (message == null) {
          throw StateError(
            'Received null event on non-nullable stream onPrinterStatusChanged',
          );
        }
        final rawPtr = Pointer<Uint8>.fromAddress(message as int);
        try {
          return PrinterStatusRecordFfiExt.fromNative(rawPtr);
        } finally {
          _nitroFree(rawPtr);
        }
      },
      release: (port) => _releaseOnPrinterStatusChangedPtr(port),
      backpressure: Backpressure.dropLatest,
    );
  }

  @override
  Stream<DiscoveredPrinter> onPrinterDiscovered() {
    checkDisposed();
    return NitroRuntime.openStream<DiscoveredPrinter>(
      register: (port) => _registerOnPrinterDiscoveredPtr(_instanceId, port),
      unpack: (message) {
        if (message == null) {
          throw StateError(
            'Received null event on non-nullable stream onPrinterDiscovered',
          );
        }
        final rawPtr = Pointer<Uint8>.fromAddress(message as int);
        try {
          return DiscoveredPrinterRecordFfiExt.fromNative(rawPtr);
        } finally {
          _nitroFree(rawPtr);
        }
      },
      release: (port) => _releaseOnPrinterDiscoveredPtr(port),
      backpressure: Backpressure.dropLatest,
    );
  }
}

/// PX19: Creates the native (dart:ffi) implementation of [NitroPrinting].
/// On web, import `*.web.bridge.g.dart` conditionally and call
/// `createNitroPrintingWebInstance()` instead.
/// A distinct [key] creates an independent native instance; the default
/// key returns the shared singleton.
NitroPrinting nitroPrinting_createNativeInstance([String key = 'default']) =>
    _NitroPrintingImpl(key);

extension NitroPrintingNativeRef on NitroPrinting {
  AnyNativeObject get asAnyNativeObject =>
      (this as _NitroPrintingImpl).asAnyNativeObject;
}

/// Creates the native implementation of [NitroPrinting]. A distinct [key]
/// creates an independent native instance; the default key returns the
/// shared singleton. On web the platform shim resolves this to the WASM
/// bridge factory instead.
NitroPrinting createNitroPrintingInstance([String key = 'default']) =>
    _NitroPrintingImpl(key);

/// No-op on native (the dynamic library loads synchronously). On web this
/// resolves to the WASM module loader and MUST be awaited before
/// [createNitroPrintingInstance].
Future<void> ensureNitroPrintingReady({String? jsUrl}) async {}
