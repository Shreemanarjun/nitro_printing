// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.4.4
// Generated from: nitro_printing.native.dart
package nitro.nitro_printing_module

import android.app.Activity
import android.content.Context
import androidx.annotation.Keep
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.launch
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.runBlocking

// --- Enums ---
@Keep
enum class PrintState(val nativeValue: Long) {
  IDLE(0),
  PRINTING(1),
  COMPLETED(2),
  CANCELLED(3),
  FAILED(4),
  PAUSED(5);

  companion object {
    fun fromNative(v: Long): PrintState = values().first { it.nativeValue == v }
  }
}

@Keep
enum class PrintQuality(val nativeValue: Long) {
  DRAFT(0),
  NORMAL(1),
  HIGH(2),
  BEST(3);

  companion object {
    fun fromNative(v: Long): PrintQuality = values().first { it.nativeValue == v }
  }
}

@Keep
enum class PaperSize(val nativeValue: Long) {
  A4(0),
  A5(1),
  LETTER(2),
  LEGAL(3),
  CUSTOM(4);

  companion object {
    fun fromNative(v: Long): PaperSize = values().first { it.nativeValue == v }
  }
}

@Keep
enum class DocumentType(val nativeValue: Long) {
  PLAINTEXT(0),
  HTML(1),
  PDF(2),
  IMAGE(3);

  companion object {
    fun fromNative(v: Long): DocumentType = values().first { it.nativeValue == v }
  }
}

@Keep
enum class MediaType(val nativeValue: Long) {
  PLAIN(0),
  GLOSSY(1),
  MATTE(2),
  PHOTO(3),
  LABEL(4),
  ENVELOPE(5);

  companion object {
    fun fromNative(v: Long): MediaType = values().first { it.nativeValue == v }
  }
}

// --- Structs ---
@androidx.annotation.Keep
data class PrintDocument(val id: String, val title: String, val type: Long, val data: ByteArray) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrintDocument {
            val id = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val title = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val type = buf.long
            val data = { val len = buf.int; val b = ByteArray(len); buf.get(b); b }()
            return PrintDocument(id, title, type, data)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrintDocument {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(id)
        writeString(title)
        writeInt(type.toLong())
        writeInt32(data.size); out.write(data)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(116)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

@androidx.annotation.Keep
data class PreviewResult(val bytes: java.nio.ByteBuffer, val length: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PreviewResult {
            val bytes = { buf.long; java.nio.ByteBuffer.allocate(0) }()
            val length = buf.long
            return PreviewResult(bytes, length)
        }
        @JvmStatic fun decode(bytes: ByteArray): PreviewResult {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(0L)
        writeInt(length)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(44)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

// --- @HybridRecord Kotlin data classes (generated by Nitrogen) ---

@androidx.annotation.Keep
data class PrinterInfo(val id: String, val name: String, val address: String, val isDefault: Boolean, val isAvailable: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrinterInfo {
            val id = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val name = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val address = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val isDefault = (buf.get().toInt() != 0)
            val isAvailable = (buf.get().toInt() != 0)
            return PrinterInfo(id, name, address, isDefault, isAvailable)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrinterInfo {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(id)
        writeString(name)
        writeString(address)
        writeBool(isDefault)
        writeBool(isAvailable)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(110)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
}

@androidx.annotation.Keep
data class PrinterCapabilities(val supportsColor: Boolean, val supportsDuplex: Boolean, val supportsCopy: Boolean, val maxCopies: Long, val minMarginTop: Double, val minMarginBottom: Double, val minMarginLeft: Double, val minMarginRight: Double, val supportsA4: Boolean, val supportsA5: Boolean, val supportsLetter: Boolean, val supportsLegal: Boolean, val supportsDraftQuality: Boolean, val supportsNormalQuality: Boolean, val supportsHighQuality: Boolean, val supportsBestQuality: Boolean, val maxResolutionDpi: Long, val supportsCustomPaper: Boolean, val supportsBorderless: Boolean, val inputTrays: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrinterCapabilities {
            val supportsColor = (buf.get().toInt() != 0)
            val supportsDuplex = (buf.get().toInt() != 0)
            val supportsCopy = (buf.get().toInt() != 0)
            val maxCopies = buf.long
            val minMarginTop = buf.double
            val minMarginBottom = buf.double
            val minMarginLeft = buf.double
            val minMarginRight = buf.double
            val supportsA4 = (buf.get().toInt() != 0)
            val supportsA5 = (buf.get().toInt() != 0)
            val supportsLetter = (buf.get().toInt() != 0)
            val supportsLegal = (buf.get().toInt() != 0)
            val supportsDraftQuality = (buf.get().toInt() != 0)
            val supportsNormalQuality = (buf.get().toInt() != 0)
            val supportsHighQuality = (buf.get().toInt() != 0)
            val supportsBestQuality = (buf.get().toInt() != 0)
            val maxResolutionDpi = buf.long
            val supportsCustomPaper = (buf.get().toInt() != 0)
            val supportsBorderless = (buf.get().toInt() != 0)
            val inputTrays = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return PrinterCapabilities(supportsColor, supportsDuplex, supportsCopy, maxCopies, minMarginTop, minMarginBottom, minMarginLeft, minMarginRight, supportsA4, supportsA5, supportsLetter, supportsLegal, supportsDraftQuality, supportsNormalQuality, supportsHighQuality, supportsBestQuality, maxResolutionDpi, supportsCustomPaper, supportsBorderless, inputTrays)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrinterCapabilities {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(supportsColor)
        writeBool(supportsDuplex)
        writeBool(supportsCopy)
        writeInt(maxCopies.toLong())
        writeDouble(minMarginTop)
        writeDouble(minMarginBottom)
        writeDouble(minMarginLeft)
        writeDouble(minMarginRight)
        writeBool(supportsA4)
        writeBool(supportsA5)
        writeBool(supportsLetter)
        writeBool(supportsLegal)
        writeBool(supportsDraftQuality)
        writeBool(supportsNormalQuality)
        writeBool(supportsHighQuality)
        writeBool(supportsBestQuality)
        writeInt(maxResolutionDpi.toLong())
        writeBool(supportsCustomPaper)
        writeBool(supportsBorderless)
        writeString(inputTrays)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(97)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
}

@androidx.annotation.Keep
data class PrintSettings(val printerId: String, val paperSize: PaperSize, val orientationDegrees: Double, val quality: PrintQuality, val copies: Long, val collate: Boolean, val duplex: Boolean, val color: Boolean, val marginTop: Double, val marginBottom: Double, val marginLeft: Double, val marginRight: Double, val jobName: String, val pagesPerSheet: Long, val showPrintDialog: Boolean, val pageRangeFrom: Long, val pageRangeTo: Long, val customPaperWidth: Double, val customPaperHeight: Double, val fitToPage: Boolean, val mediaType: MediaType, val headerText: String, val footerText: String, val inputTray: String, val networkTimeoutSeconds: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrintSettings {
            val printerId = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val paperSize = PaperSize.fromNative(buf.long)
            val orientationDegrees = buf.double
            val quality = PrintQuality.fromNative(buf.long)
            val copies = buf.long
            val collate = (buf.get().toInt() != 0)
            val duplex = (buf.get().toInt() != 0)
            val color = (buf.get().toInt() != 0)
            val marginTop = buf.double
            val marginBottom = buf.double
            val marginLeft = buf.double
            val marginRight = buf.double
            val jobName = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val pagesPerSheet = buf.long
            val showPrintDialog = (buf.get().toInt() != 0)
            val pageRangeFrom = buf.long
            val pageRangeTo = buf.long
            val customPaperWidth = buf.double
            val customPaperHeight = buf.double
            val fitToPage = (buf.get().toInt() != 0)
            val mediaType = MediaType.fromNative(buf.long)
            val headerText = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val footerText = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val inputTray = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val networkTimeoutSeconds = buf.long
            return PrintSettings(printerId, paperSize, orientationDegrees, quality, copies, collate, duplex, color, marginTop, marginBottom, marginLeft, marginRight, jobName, pagesPerSheet, showPrintDialog, pageRangeFrom, pageRangeTo, customPaperWidth, customPaperHeight, fitToPage, mediaType, headerText, footerText, inputTray, networkTimeoutSeconds)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrintSettings {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(printerId)
        writeInt(paperSize.nativeValue)
        writeDouble(orientationDegrees)
        writeInt(quality.nativeValue)
        writeInt(copies.toLong())
        writeBool(collate)
        writeBool(duplex)
        writeBool(color)
        writeDouble(marginTop)
        writeDouble(marginBottom)
        writeDouble(marginLeft)
        writeDouble(marginRight)
        writeString(jobName)
        writeInt(pagesPerSheet.toLong())
        writeBool(showPrintDialog)
        writeInt(pageRangeFrom.toLong())
        writeInt(pageRangeTo.toLong())
        writeDouble(customPaperWidth)
        writeDouble(customPaperHeight)
        writeBool(fitToPage)
        writeInt(mediaType.nativeValue)
        writeString(headerText)
        writeString(footerText)
        writeString(inputTray)
        writeInt(networkTimeoutSeconds.toLong())
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(305)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
}

@androidx.annotation.Keep
data class PrintJob(val id: String, val printerId: String, val documentTitle: String, val state: PrintState, val progress: Long, val createdAtMillis: Long, val completedAtMillis: Long, val errorMessage: String, val pagesPrinted: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrintJob {
            val id = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val printerId = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val documentTitle = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val state = PrintState.fromNative(buf.long)
            val progress = buf.long
            val createdAtMillis = buf.long
            val completedAtMillis = buf.long
            val errorMessage = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val pagesPrinted = buf.long
            return PrintJob(id, printerId, documentTitle, state, progress, createdAtMillis, completedAtMillis, errorMessage, pagesPrinted)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrintJob {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(id)
        writeString(printerId)
        writeString(documentTitle)
        writeInt(state.nativeValue)
        writeInt(progress.toLong())
        writeInt(createdAtMillis.toLong())
        writeInt(completedAtMillis.toLong())
        writeString(errorMessage)
        writeInt(pagesPrinted.toLong())
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(184)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
}

@androidx.annotation.Keep
data class PrintResult(val success: Boolean, val jobId: String, val errorMessage: String, val errorCode: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrintResult {
            val success = (buf.get().toInt() != 0)
            val jobId = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val errorMessage = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val errorCode = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return PrintResult(success, jobId, errorMessage, errorCode)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrintResult {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(success)
        writeString(jobId)
        writeString(errorMessage)
        writeString(errorCode)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(109)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
}

@androidx.annotation.Keep
data class PrintJobUpdate(val jobId: String, val state: PrintState, val progress: Long, val message: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrintJobUpdate {
            val jobId = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val state = PrintState.fromNative(buf.long)
            val progress = buf.long
            val message = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return PrintJobUpdate(jobId, state, progress, message)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrintJobUpdate {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(jobId)
        writeInt(state.nativeValue)
        writeInt(progress.toLong())
        writeString(message)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(88)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
}

@androidx.annotation.Keep
data class PrinterStatus(val printerId: String, val isOnline: Boolean, val isPrinting: Boolean, val jobsInQueue: Long, val statusMessage: String, val inkLevel: Long, val tonerLevel: Long, val paperLevel: Long, val errorCode: String, val isWarmingUp: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrinterStatus {
            val printerId = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val isOnline = (buf.get().toInt() != 0)
            val isPrinting = (buf.get().toInt() != 0)
            val jobsInQueue = buf.long
            val statusMessage = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val inkLevel = buf.long
            val tonerLevel = buf.long
            val paperLevel = buf.long
            val errorCode = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val isWarmingUp = (buf.get().toInt() != 0)
            return PrinterStatus(printerId, isOnline, isPrinting, jobsInQueue, statusMessage, inkLevel, tonerLevel, paperLevel, errorCode, isWarmingUp)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrinterStatus {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(printerId)
        writeBool(isOnline)
        writeBool(isPrinting)
        writeInt(jobsInQueue.toLong())
        writeString(statusMessage)
        writeInt(inkLevel.toLong())
        writeInt(tonerLevel.toLong())
        writeInt(paperLevel.toLong())
        writeString(errorCode)
        writeBool(isWarmingUp)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(143)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
}

@androidx.annotation.Keep
data class DiscoveredPrinter(val id: String, val name: String, val host: String, val port: Long, val serviceType: String, val uri: String, val isAvailable: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): DiscoveredPrinter {
            val id = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val name = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val host = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val port = buf.long
            val serviceType = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val uri = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val isAvailable = (buf.get().toInt() != 0)
            return DiscoveredPrinter(id, name, host, port, serviceType, uri, isAvailable)
        }
        @JvmStatic fun decode(bytes: ByteArray): DiscoveredPrinter {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(id)
        writeString(name)
        writeString(host)
        writeInt(port.toLong())
        writeString(serviceType)
        writeString(uri)
        writeBool(isAvailable)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(189)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
}

@androidx.annotation.Keep
data class PrinterStatusDetail(val printerId: String, val isOnline: Boolean, val isReady: Boolean, val hasPaperJam: Boolean, val isOutOfPaper: Boolean, val isOutOfInk: Boolean, val inkLevelBlack: Long, val inkLevelCyan: Long, val inkLevelMagenta: Long, val inkLevelYellow: Long, val tonerLevel: Long, val paperLevel: Long, val jobsInQueue: Long, val isWarmingUp: Boolean, val printerState: String, val stateReasons: String, val statusMessage: String, val errorCode: String, val isDuplexSupported: Boolean, val isColorSupported: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrinterStatusDetail {
            val printerId = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val isOnline = (buf.get().toInt() != 0)
            val isReady = (buf.get().toInt() != 0)
            val hasPaperJam = (buf.get().toInt() != 0)
            val isOutOfPaper = (buf.get().toInt() != 0)
            val isOutOfInk = (buf.get().toInt() != 0)
            val inkLevelBlack = buf.long
            val inkLevelCyan = buf.long
            val inkLevelMagenta = buf.long
            val inkLevelYellow = buf.long
            val tonerLevel = buf.long
            val paperLevel = buf.long
            val jobsInQueue = buf.long
            val isWarmingUp = (buf.get().toInt() != 0)
            val printerState = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val stateReasons = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val statusMessage = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val errorCode = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val isDuplexSupported = (buf.get().toInt() != 0)
            val isColorSupported = (buf.get().toInt() != 0)
            return PrinterStatusDetail(printerId, isOnline, isReady, hasPaperJam, isOutOfPaper, isOutOfInk, inkLevelBlack, inkLevelCyan, inkLevelMagenta, inkLevelYellow, tonerLevel, paperLevel, jobsInQueue, isWarmingUp, printerState, stateReasons, statusMessage, errorCode, isDuplexSupported, isColorSupported)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrinterStatusDetail {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(printerId)
        writeBool(isOnline)
        writeBool(isReady)
        writeBool(hasPaperJam)
        writeBool(isOutOfPaper)
        writeBool(isOutOfInk)
        writeInt(inkLevelBlack.toLong())
        writeInt(inkLevelCyan.toLong())
        writeInt(inkLevelMagenta.toLong())
        writeInt(inkLevelYellow.toLong())
        writeInt(tonerLevel.toLong())
        writeInt(paperLevel.toLong())
        writeInt(jobsInQueue.toLong())
        writeBool(isWarmingUp)
        writeString(printerState)
        writeString(stateReasons)
        writeString(statusMessage)
        writeString(errorCode)
        writeBool(isDuplexSupported)
        writeBool(isColorSupported)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(244)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
}

@androidx.annotation.Keep
data class NitroNullableInt(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableInt {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroNullableInt(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableInt {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(9)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
}

@androidx.annotation.Keep
data class NitroNullableDouble(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableDouble {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroNullableDouble(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableDouble {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(9)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
}

@androidx.annotation.Keep
data class NitroNullableBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroNullableBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(2)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
}

/**
 * Contract for the [NitroPrinting] module.
 * Implement this in your Kotlin source code.
 * Nitro may call this implementation from any JNI thread.
 * Keep mutable state thread-safe or marshal work onto your own dispatcher.
 */
interface HybridNitroPrintingSpec {
    val applicationContext: Context get() = NitroPrintingJniBridge.applicationContext
    val activity: Activity? get() = NitroPrintingJniBridge.activity

    // Optional lifecycle hooks — override only what you need.
    fun onAttached() {}
    fun onDetached() {}
    fun onActivityAttached(activity: Activity) {}
    fun onActivityDetached() {}

    // source: nitro_printing.native.dart:17
    fun isPrintingSupported(): Boolean
    // source: nitro_printing.native.dart:18
    fun getPrintersCount(): Long
    // source: nitro_printing.native.dart:19
    fun getPrinterDriverVersion(printerId: String): String
    // source: nitro_printing.native.dart:25
    suspend fun getAllPrinters(): List<PrinterInfo>
    // source: nitro_printing.native.dart:28
    suspend fun getPrinterAt(index: Long): PrinterInfo
    // source: nitro_printing.native.dart:31
    suspend fun getDefaultPrinter(): PrinterInfo
    // source: nitro_printing.native.dart:34
    suspend fun getPrinterCapabilities(printerId: String): PrinterCapabilities
    // source: nitro_printing.native.dart:39
    suspend fun printText(text: String, settings: PrintSettings?): PrintResult
    // source: nitro_printing.native.dart:42
    suspend fun printImage(imageData: ByteArray, settings: PrintSettings?): PrintResult
    // source: nitro_printing.native.dart:48
    suspend fun printPdf(pdfData: ByteArray, settings: PrintSettings?): PrintResult
    // source: nitro_printing.native.dart:51
    suspend fun printDocument(document: PrintDocument, settings: PrintSettings?): PrintResult
    // source: nitro_printing.native.dart:57
    suspend fun printFile(filePath: String, settings: PrintSettings?): Boolean
    // source: nitro_printing.native.dart:63
    suspend fun renderPreview(document: PrintDocument, settings: PrintSettings?): PreviewResult
    // source: nitro_printing.native.dart:70
    suspend fun getPageCount(document: PrintDocument): Long
    // source: nitro_printing.native.dart:74
    suspend fun printToFile(document: PrintDocument, outputPath: String, settings: PrintSettings?): Boolean
    // source: nitro_printing.native.dart:83
    suspend fun cancelPrintJob(jobId: String): Boolean
    // source: nitro_printing.native.dart:86
    suspend fun pausePrintJob(jobId: String): Boolean
    // source: nitro_printing.native.dart:89
    suspend fun resumePrintJob(jobId: String): Boolean
    // source: nitro_printing.native.dart:92
    suspend fun clearPrintQueue(): Boolean
    // source: nitro_printing.native.dart:95
    suspend fun getPrintJobsCount(): Long
    // source: nitro_printing.native.dart:98
    suspend fun getPrintJobAt(index: Long): PrintJob
    // source: nitro_printing.native.dart:102
    suspend fun getPrintJobStatus(jobId: String): PrintJob
    // source: nitro_printing.native.dart:109
    suspend fun startPrinterDiscovery(): Boolean
    // source: nitro_printing.native.dart:112
    suspend fun stopPrinterDiscovery(): Boolean
    // source: nitro_printing.native.dart:118
    suspend fun testPrinterConnection(printerId: String, timeoutSeconds: Long?): Boolean
    // source: nitro_printing.native.dart:122
    suspend fun setDefaultPrinter(printerId: String): Boolean
    // source: nitro_printing.native.dart:132
    suspend fun openSystemPrintQueue(printerId: String): Boolean
    // source: nitro_printing.native.dart:136
    suspend fun openPrinterProperties(printerId: String): Boolean
    // source: nitro_printing.native.dart:144
    suspend fun printRaw(data: ByteArray, settings: PrintSettings?): PrintResult
    // source: nitro_printing.native.dart:149
    suspend fun printEscPos(escPosData: ByteArray, settings: PrintSettings?): PrintResult
    // source: nitro_printing.native.dart:157
    suspend fun printZpl(zpl: String, settings: PrintSettings?): PrintResult
    // source: nitro_printing.native.dart:163
    suspend fun cancelRawPrint(): Boolean
    // source: nitro_printing.native.dart:170
    suspend fun getPrinterStatusDetail(printerId: String, timeoutSeconds: Long?): PrinterStatusDetail
    val onPrintJobChanged: Flow<PrintJobUpdate>
    val onPrinterStatusChanged: Flow<PrinterStatus>
    val onPrinterDiscovered: Flow<DiscoveredPrinter>
}

@Keep
object NitroPrintingJniBridge {
    private var implementation: HybridNitroPrintingSpec? = null
    private val _asyncExecutor = java.util.concurrent.Executors.newCachedThreadPool()

    lateinit var applicationContext: Context
        private set

    var activity: Activity? = null
        private set

    @JvmStatic external fun initialize(bridgeClass: Class<*>)

    fun register(impl: HybridNitroPrintingSpec, context: Context) {
        applicationContext = context
        implementation = impl
        initialize(this::class.java)
        impl.onAttached()
    }

    fun onDetached() {
        implementation?.onDetached()
        activity = null
        implementation = null
    }

    fun onActivityAttached(newActivity: Activity) {
        activity = newActivity
        implementation?.onActivityAttached(newActivity)
    }

    fun onActivityDetached() {
        activity = null
        implementation?.onActivityDetached()
    }

    // source: nitro_printing.native.dart:17
    @JvmStatic fun isPrintingSupported_call(): Boolean {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return impl.isPrintingSupported()
    }
    // source: nitro_printing.native.dart:18
    @JvmStatic fun getPrintersCount_call(): Long {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return impl.getPrintersCount()
    }
    // source: nitro_printing.native.dart:19
    @JvmStatic fun getPrinterDriverVersion_call(printerId: String): String {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return impl.getPrinterDriverVersion(printerId)
    }
    // source: nitro_printing.native.dart:25
    @JvmStatic fun getAllPrinters_call(): ByteArray {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.getAllPrinters() } }).get()
        val itemBufs = ArrayList<ByteArray>(result.size)
        val tmpBuf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        for (item in result) {
            val tmpOut = java.io.ByteArrayOutputStream(110)
            item.writeFieldsTo(tmpOut, tmpBuf)
            itemBufs.add(tmpOut.toByteArray())
        }
        // payload = [4B count][8B × n offsets][item bytes...]
        var offsetPos = 4 + 8L * result.size  // start of item data in payload
        val offsets = LongArray(result.size)
        for (i in result.indices) { offsets[i] = offsetPos; offsetPos += itemBufs[i].size }
        val payloadBuf = java.nio.ByteBuffer.allocate(offsetPos.toInt()).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        payloadBuf.putInt(result.size)
        offsets.forEach { payloadBuf.putLong(it) }
        itemBufs.forEach { payloadBuf.put(it) }
        val payload = payloadBuf.array()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
    // source: nitro_printing.native.dart:28
    @JvmStatic fun getPrinterAt_call(index: Long): ByteArray {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.getPrinterAt(index) } }).get()
        return result.encode()
    }
    // source: nitro_printing.native.dart:31
    @JvmStatic fun getDefaultPrinter_call(): ByteArray {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.getDefaultPrinter() } }).get()
        return result.encode()
    }
    // source: nitro_printing.native.dart:34
    @JvmStatic fun getPrinterCapabilities_call(printerId: String): ByteArray {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.getPrinterCapabilities(printerId) } }).get()
        return result.encode()
    }
    // source: nitro_printing.native.dart:39
    @JvmStatic fun printText_call(text: String, settings: ByteArray): ByteArray {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val settingsBuf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        settingsBuf.getInt() // skip Dart 4-byte outer length prefix
        val settingsDecoded = PrintSettings.decodeFrom(settingsBuf)
        val result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.printText(text, settingsDecoded) } }).get()
        return result.encode()
    }
    // source: nitro_printing.native.dart:42
    @JvmStatic fun printImage_call(imageData: ByteArray, settings: ByteArray): ByteArray {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val settingsBuf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        settingsBuf.getInt() // skip Dart 4-byte outer length prefix
        val settingsDecoded = PrintSettings.decodeFrom(settingsBuf)
        val result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.printImage(imageData, settingsDecoded) } }).get()
        return result.encode()
    }
    // source: nitro_printing.native.dart:48
    @JvmStatic fun printPdf_call(pdfData: ByteArray, settings: ByteArray): ByteArray {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val settingsBuf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        settingsBuf.getInt() // skip Dart 4-byte outer length prefix
        val settingsDecoded = PrintSettings.decodeFrom(settingsBuf)
        val result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.printPdf(pdfData, settingsDecoded) } }).get()
        return result.encode()
    }
    // source: nitro_printing.native.dart:51
    @JvmStatic fun printDocument_call(document: PrintDocument, settings: ByteArray): ByteArray {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val settingsBuf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        settingsBuf.getInt() // skip Dart 4-byte outer length prefix
        val settingsDecoded = PrintSettings.decodeFrom(settingsBuf)
        val result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.printDocument(document, settingsDecoded) } }).get()
        return result.encode()
    }
    // source: nitro_printing.native.dart:57
    @JvmStatic fun printFile_call(filePath: String, settings: ByteArray): Boolean {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val settingsBuf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        settingsBuf.getInt() // skip Dart 4-byte outer length prefix
        val settingsDecoded = PrintSettings.decodeFrom(settingsBuf)
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.printFile(filePath, settingsDecoded) }
        }).get()
    }
    // source: nitro_printing.native.dart:63
    @JvmStatic fun renderPreview_call(document: PrintDocument, settings: ByteArray): PreviewResult {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val settingsBuf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        settingsBuf.getInt() // skip Dart 4-byte outer length prefix
        val settingsDecoded = PrintSettings.decodeFrom(settingsBuf)
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.renderPreview(document, settingsDecoded) }
        }).get()
    }
    // source: nitro_printing.native.dart:70
    @JvmStatic fun getPageCount_call(document: PrintDocument): Long {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.getPageCount(document) }
        }).get()
    }
    // source: nitro_printing.native.dart:74
    @JvmStatic fun printToFile_call(document: PrintDocument, outputPath: String, settings: ByteArray): Boolean {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val settingsBuf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        settingsBuf.getInt() // skip Dart 4-byte outer length prefix
        val settingsDecoded = PrintSettings.decodeFrom(settingsBuf)
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.printToFile(document, outputPath, settingsDecoded) }
        }).get()
    }
    // source: nitro_printing.native.dart:83
    @JvmStatic fun cancelPrintJob_call(jobId: String): Boolean {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.cancelPrintJob(jobId) }
        }).get()
    }
    // source: nitro_printing.native.dart:86
    @JvmStatic fun pausePrintJob_call(jobId: String): Boolean {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.pausePrintJob(jobId) }
        }).get()
    }
    // source: nitro_printing.native.dart:89
    @JvmStatic fun resumePrintJob_call(jobId: String): Boolean {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.resumePrintJob(jobId) }
        }).get()
    }
    // source: nitro_printing.native.dart:92
    @JvmStatic fun clearPrintQueue_call(): Boolean {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.clearPrintQueue() }
        }).get()
    }
    // source: nitro_printing.native.dart:95
    @JvmStatic fun getPrintJobsCount_call(): Long {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.getPrintJobsCount() }
        }).get()
    }
    // source: nitro_printing.native.dart:98
    @JvmStatic fun getPrintJobAt_call(index: Long): ByteArray {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.getPrintJobAt(index) } }).get()
        return result.encode()
    }
    // source: nitro_printing.native.dart:102
    @JvmStatic fun getPrintJobStatus_call(jobId: String): ByteArray {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.getPrintJobStatus(jobId) } }).get()
        return result.encode()
    }
    // source: nitro_printing.native.dart:109
    @JvmStatic fun startPrinterDiscovery_call(): Boolean {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.startPrinterDiscovery() }
        }).get()
    }
    // source: nitro_printing.native.dart:112
    @JvmStatic fun stopPrinterDiscovery_call(): Boolean {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.stopPrinterDiscovery() }
        }).get()
    }
    // source: nitro_printing.native.dart:118
    @JvmStatic fun testPrinterConnection_call(printerId: String, timeoutSeconds: Long): Boolean {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        // Dart layer sends Long.MIN_VALUE as sentinel when caller passes null for timeoutSeconds.
        val timeoutSecondsArg: Long? = if (timeoutSeconds == Long.MIN_VALUE) null else timeoutSeconds
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.testPrinterConnection(printerId, timeoutSecondsArg) }
        }).get()
    }
    // source: nitro_printing.native.dart:122
    @JvmStatic fun setDefaultPrinter_call(printerId: String): Boolean {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.setDefaultPrinter(printerId) }
        }).get()
    }
    // source: nitro_printing.native.dart:132
    @JvmStatic fun openSystemPrintQueue_call(printerId: String): Boolean {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.openSystemPrintQueue(printerId) }
        }).get()
    }
    // source: nitro_printing.native.dart:136
    @JvmStatic fun openPrinterProperties_call(printerId: String): Boolean {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.openPrinterProperties(printerId) }
        }).get()
    }
    // source: nitro_printing.native.dart:144
    @JvmStatic fun printRaw_call(data: ByteArray, settings: ByteArray): ByteArray {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val settingsBuf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        settingsBuf.getInt() // skip Dart 4-byte outer length prefix
        val settingsDecoded = PrintSettings.decodeFrom(settingsBuf)
        val result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.printRaw(data, settingsDecoded) } }).get()
        return result.encode()
    }
    // source: nitro_printing.native.dart:149
    @JvmStatic fun printEscPos_call(escPosData: ByteArray, settings: ByteArray): ByteArray {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val settingsBuf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        settingsBuf.getInt() // skip Dart 4-byte outer length prefix
        val settingsDecoded = PrintSettings.decodeFrom(settingsBuf)
        val result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.printEscPos(escPosData, settingsDecoded) } }).get()
        return result.encode()
    }
    // source: nitro_printing.native.dart:157
    @JvmStatic fun printZpl_call(zpl: String, settings: ByteArray): ByteArray {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        val settingsBuf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        settingsBuf.getInt() // skip Dart 4-byte outer length prefix
        val settingsDecoded = PrintSettings.decodeFrom(settingsBuf)
        val result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.printZpl(zpl, settingsDecoded) } }).get()
        return result.encode()
    }
    // source: nitro_printing.native.dart:163
    @JvmStatic fun cancelRawPrint_call(): Boolean {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.cancelRawPrint() }
        }).get()
    }
    // source: nitro_printing.native.dart:170
    @JvmStatic fun getPrinterStatusDetail_call(printerId: String, timeoutSeconds: Long): ByteArray {
        val impl = implementation ?: throw IllegalStateException("NitroPrinting not registered")
        // Dart layer sends Long.MIN_VALUE as sentinel when caller passes null for timeoutSeconds.
        val timeoutSecondsArg: Long? = if (timeoutSeconds == Long.MIN_VALUE) null else timeoutSeconds
        val result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.getPrinterStatusDetail(printerId, timeoutSecondsArg) } }).get()
        return result.encode()
    }
    private val _streamJobs = java.util.concurrent.ConcurrentHashMap<Pair<String, Long>, kotlinx.coroutines.Job>()

    @JvmStatic external fun emit_onPrintJobChanged(dartPort: Long, item: PrintJobUpdate): Boolean

    @JvmStatic fun nitro_printing_register_on_print_job_changed_stream_call(dartPort: Long) {
        val impl = implementation ?: return
        _streamJobs[Pair("onPrintJobChanged", dartPort)] = CoroutineScope(Dispatchers.Default).launch {
            impl.onPrintJobChanged.collect { item -> 
                if (!emit_onPrintJobChanged(dartPort, item)) {
                    _streamJobs.remove(Pair("onPrintJobChanged", dartPort))?.cancel()
                    return@collect
                }
            }
        }
    }
    @JvmStatic fun nitro_printing_release_on_print_job_changed_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("onPrintJobChanged", dartPort))?.cancel()
    }
    @JvmStatic external fun emit_onPrinterStatusChanged(dartPort: Long, item: PrinterStatus): Boolean

    @JvmStatic fun nitro_printing_register_on_printer_status_changed_stream_call(dartPort: Long) {
        val impl = implementation ?: return
        _streamJobs[Pair("onPrinterStatusChanged", dartPort)] = CoroutineScope(Dispatchers.Default).launch {
            impl.onPrinterStatusChanged.collect { item -> 
                if (!emit_onPrinterStatusChanged(dartPort, item)) {
                    _streamJobs.remove(Pair("onPrinterStatusChanged", dartPort))?.cancel()
                    return@collect
                }
            }
        }
    }
    @JvmStatic fun nitro_printing_release_on_printer_status_changed_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("onPrinterStatusChanged", dartPort))?.cancel()
    }
    @JvmStatic external fun emit_onPrinterDiscovered(dartPort: Long, item: DiscoveredPrinter): Boolean

    @JvmStatic fun nitro_printing_register_on_printer_discovered_stream_call(dartPort: Long) {
        val impl = implementation ?: return
        _streamJobs[Pair("onPrinterDiscovered", dartPort)] = CoroutineScope(Dispatchers.Default).launch {
            impl.onPrinterDiscovered.collect { item -> 
                if (!emit_onPrinterDiscovered(dartPort, item)) {
                    _streamJobs.remove(Pair("onPrinterDiscovered", dartPort))?.cancel()
                    return@collect
                }
            }
        }
    }
    @JvmStatic fun nitro_printing_release_on_printer_discovered_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("onPrinterDiscovered", dartPort))?.cancel()
    }
}
