// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.5.10
// Generated from: nitro_printing.native.dart
package nitro.nitro_printing_module

import android.app.Activity
import android.content.Context
import androidx.annotation.Keep
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.launch
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.CoroutineStart
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.runBlocking

// --- Enums ---
@Keep
enum class PrintState(val nativeValue: Long) {
  IDLE(0),
  PRINTING(1),
  COMPLETED(2),
  CANCELLED(3),
  FAILED(4),
  PAUSED(5);

  companion object {
    fun fromNative(v: Long): PrintState = values().first { it.nativeValue == v }
  }
}

@Keep
enum class PrintQuality(val nativeValue: Long) {
  DRAFT(0),
  NORMAL(1),
  HIGH(2),
  BEST(3);

  companion object {
    fun fromNative(v: Long): PrintQuality = values().first { it.nativeValue == v }
  }
}

@Keep
enum class PaperSize(val nativeValue: Long) {
  A4(0),
  A5(1),
  LETTER(2),
  LEGAL(3),
  CUSTOM(4);

  companion object {
    fun fromNative(v: Long): PaperSize = values().first { it.nativeValue == v }
  }
}

@Keep
enum class DocumentType(val nativeValue: Long) {
  PLAINTEXT(0),
  HTML(1),
  PDF(2),
  IMAGE(3);

  companion object {
    fun fromNative(v: Long): DocumentType = values().first { it.nativeValue == v }
  }
}

@Keep
enum class MediaType(val nativeValue: Long) {
  PLAIN(0),
  GLOSSY(1),
  MATTE(2),
  PHOTO(3),
  LABEL(4),
  ENVELOPE(5);

  companion object {
    fun fromNative(v: Long): MediaType = values().first { it.nativeValue == v }
  }
}

@Keep
enum class PrintDialogState(val nativeValue: Long) {
  IDLE(0),
  SHOWING(1),
  CONFIRMED(2),
  CANCELLED(3);

  companion object {
    fun fromNative(v: Long): PrintDialogState = values().first { it.nativeValue == v }
  }
}

// --- Structs ---
@androidx.annotation.Keep
data class PreviewResult(val bytes: java.nio.ByteBuffer, val length: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PreviewResult {
            val bytes = { buf.long; java.nio.ByteBuffer.allocate(0) }()
            val length = buf.long
            return PreviewResult(bytes, length)
        }
        @JvmStatic fun decode(bytes: ByteArray): PreviewResult {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(0L)
        writeInt(length)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(44)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

// --- @HybridRecord Kotlin data classes (generated by Nitrogen) ---

@androidx.annotation.Keep
data class PrinterInfo(val id: String, val name: String, val address: String, val isDefault: Boolean, val isAvailable: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrinterInfo {
            val id = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val name = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val address = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val isDefault = (buf.get().toInt() != 0)
            val isAvailable = (buf.get().toInt() != 0)
            return PrinterInfo(id, name, address, isDefault, isAvailable)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrinterInfo {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): PrinterInfo = PrinterInfo(
                id = map["id"] as String,
                name = map["name"] as String,
                address = map["address"] as String,
                isDefault = map["isDefault"] as Boolean,
                isAvailable = map["isAvailable"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(110) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(id)
        writeString(name)
        writeString(address)
        writeBool(isDefault)
        writeBool(isAvailable)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, PrinterInfo> support ---
    fun toJson(): Map<String, Any?> = mapOf("id" to id, "name" to name, "address" to address, "isDefault" to isDefault, "isAvailable" to isAvailable)

}

@androidx.annotation.Keep
data class PrinterCapabilities(val supportsColor: Boolean, val supportsDuplex: Boolean, val supportsCopy: Boolean, val maxCopies: Long, val minMarginTop: Double, val minMarginBottom: Double, val minMarginLeft: Double, val minMarginRight: Double, val supportsA4: Boolean, val supportsA5: Boolean, val supportsLetter: Boolean, val supportsLegal: Boolean, val supportsDraftQuality: Boolean, val supportsNormalQuality: Boolean, val supportsHighQuality: Boolean, val supportsBestQuality: Boolean, val maxResolutionDpi: Long, val supportsCustomPaper: Boolean, val supportsBorderless: Boolean, val inputTrays: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrinterCapabilities {
            val supportsColor = (buf.get().toInt() != 0)
            val supportsDuplex = (buf.get().toInt() != 0)
            val supportsCopy = (buf.get().toInt() != 0)
            val maxCopies = buf.long
            val minMarginTop = buf.double
            val minMarginBottom = buf.double
            val minMarginLeft = buf.double
            val minMarginRight = buf.double
            val supportsA4 = (buf.get().toInt() != 0)
            val supportsA5 = (buf.get().toInt() != 0)
            val supportsLetter = (buf.get().toInt() != 0)
            val supportsLegal = (buf.get().toInt() != 0)
            val supportsDraftQuality = (buf.get().toInt() != 0)
            val supportsNormalQuality = (buf.get().toInt() != 0)
            val supportsHighQuality = (buf.get().toInt() != 0)
            val supportsBestQuality = (buf.get().toInt() != 0)
            val maxResolutionDpi = buf.long
            val supportsCustomPaper = (buf.get().toInt() != 0)
            val supportsBorderless = (buf.get().toInt() != 0)
            val inputTrays = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return PrinterCapabilities(supportsColor, supportsDuplex, supportsCopy, maxCopies, minMarginTop, minMarginBottom, minMarginLeft, minMarginRight, supportsA4, supportsA5, supportsLetter, supportsLegal, supportsDraftQuality, supportsNormalQuality, supportsHighQuality, supportsBestQuality, maxResolutionDpi, supportsCustomPaper, supportsBorderless, inputTrays)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrinterCapabilities {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): PrinterCapabilities = PrinterCapabilities(
                supportsColor = map["supportsColor"] as Boolean,
                supportsDuplex = map["supportsDuplex"] as Boolean,
                supportsCopy = map["supportsCopy"] as Boolean,
                maxCopies = (map["maxCopies"] as Number).toLong(),
                minMarginTop = (map["minMarginTop"] as Number).toDouble(),
                minMarginBottom = (map["minMarginBottom"] as Number).toDouble(),
                minMarginLeft = (map["minMarginLeft"] as Number).toDouble(),
                minMarginRight = (map["minMarginRight"] as Number).toDouble(),
                supportsA4 = map["supportsA4"] as Boolean,
                supportsA5 = map["supportsA5"] as Boolean,
                supportsLetter = map["supportsLetter"] as Boolean,
                supportsLegal = map["supportsLegal"] as Boolean,
                supportsDraftQuality = map["supportsDraftQuality"] as Boolean,
                supportsNormalQuality = map["supportsNormalQuality"] as Boolean,
                supportsHighQuality = map["supportsHighQuality"] as Boolean,
                supportsBestQuality = map["supportsBestQuality"] as Boolean,
                maxResolutionDpi = (map["maxResolutionDpi"] as Number).toLong(),
                supportsCustomPaper = map["supportsCustomPaper"] as Boolean,
                supportsBorderless = map["supportsBorderless"] as Boolean,
                inputTrays = map["inputTrays"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(97) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(supportsColor)
        writeBool(supportsDuplex)
        writeBool(supportsCopy)
        writeInt(maxCopies.toLong())
        writeDouble(minMarginTop)
        writeDouble(minMarginBottom)
        writeDouble(minMarginLeft)
        writeDouble(minMarginRight)
        writeBool(supportsA4)
        writeBool(supportsA5)
        writeBool(supportsLetter)
        writeBool(supportsLegal)
        writeBool(supportsDraftQuality)
        writeBool(supportsNormalQuality)
        writeBool(supportsHighQuality)
        writeBool(supportsBestQuality)
        writeInt(maxResolutionDpi.toLong())
        writeBool(supportsCustomPaper)
        writeBool(supportsBorderless)
        writeString(inputTrays)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, PrinterCapabilities> support ---
    fun toJson(): Map<String, Any?> = mapOf("supportsColor" to supportsColor, "supportsDuplex" to supportsDuplex, "supportsCopy" to supportsCopy, "maxCopies" to maxCopies, "minMarginTop" to minMarginTop, "minMarginBottom" to minMarginBottom, "minMarginLeft" to minMarginLeft, "minMarginRight" to minMarginRight, "supportsA4" to supportsA4, "supportsA5" to supportsA5, "supportsLetter" to supportsLetter, "supportsLegal" to supportsLegal, "supportsDraftQuality" to supportsDraftQuality, "supportsNormalQuality" to supportsNormalQuality, "supportsHighQuality" to supportsHighQuality, "supportsBestQuality" to supportsBestQuality, "maxResolutionDpi" to maxResolutionDpi, "supportsCustomPaper" to supportsCustomPaper, "supportsBorderless" to supportsBorderless, "inputTrays" to inputTrays)

}

@androidx.annotation.Keep
data class PrintSettings(val printerId: String, val paperSize: PaperSize, val orientationDegrees: Double, val quality: PrintQuality, val copies: Long, val collate: Boolean, val duplex: Boolean, val color: Boolean, val marginTop: Double, val marginBottom: Double, val marginLeft: Double, val marginRight: Double, val jobName: String, val pagesPerSheet: Long, val showPrintDialog: Boolean, val pageRangeFrom: Long, val pageRangeTo: Long, val customPaperWidth: Double, val customPaperHeight: Double, val fitToPage: Boolean, val mediaType: MediaType, val headerText: String, val footerText: String, val inputTray: String, val networkTimeoutSeconds: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrintSettings {
            val printerId = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val paperSize = PaperSize.fromNative(buf.long)
            val orientationDegrees = buf.double
            val quality = PrintQuality.fromNative(buf.long)
            val copies = buf.long
            val collate = (buf.get().toInt() != 0)
            val duplex = (buf.get().toInt() != 0)
            val color = (buf.get().toInt() != 0)
            val marginTop = buf.double
            val marginBottom = buf.double
            val marginLeft = buf.double
            val marginRight = buf.double
            val jobName = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val pagesPerSheet = buf.long
            val showPrintDialog = (buf.get().toInt() != 0)
            val pageRangeFrom = buf.long
            val pageRangeTo = buf.long
            val customPaperWidth = buf.double
            val customPaperHeight = buf.double
            val fitToPage = (buf.get().toInt() != 0)
            val mediaType = MediaType.fromNative(buf.long)
            val headerText = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val footerText = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val inputTray = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val networkTimeoutSeconds = buf.long
            return PrintSettings(printerId, paperSize, orientationDegrees, quality, copies, collate, duplex, color, marginTop, marginBottom, marginLeft, marginRight, jobName, pagesPerSheet, showPrintDialog, pageRangeFrom, pageRangeTo, customPaperWidth, customPaperHeight, fitToPage, mediaType, headerText, footerText, inputTray, networkTimeoutSeconds)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrintSettings {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): PrintSettings = PrintSettings(
                printerId = map["printerId"] as String,
                paperSize = PaperSize.fromNative(map["paperSize"] as Long),
                orientationDegrees = (map["orientationDegrees"] as Number).toDouble(),
                quality = PrintQuality.fromNative(map["quality"] as Long),
                copies = (map["copies"] as Number).toLong(),
                collate = map["collate"] as Boolean,
                duplex = map["duplex"] as Boolean,
                color = map["color"] as Boolean,
                marginTop = (map["marginTop"] as Number).toDouble(),
                marginBottom = (map["marginBottom"] as Number).toDouble(),
                marginLeft = (map["marginLeft"] as Number).toDouble(),
                marginRight = (map["marginRight"] as Number).toDouble(),
                jobName = map["jobName"] as String,
                pagesPerSheet = (map["pagesPerSheet"] as Number).toLong(),
                showPrintDialog = map["showPrintDialog"] as Boolean,
                pageRangeFrom = (map["pageRangeFrom"] as Number).toLong(),
                pageRangeTo = (map["pageRangeTo"] as Number).toLong(),
                customPaperWidth = (map["customPaperWidth"] as Number).toDouble(),
                customPaperHeight = (map["customPaperHeight"] as Number).toDouble(),
                fitToPage = map["fitToPage"] as Boolean,
                mediaType = MediaType.fromNative(map["mediaType"] as Long),
                headerText = map["headerText"] as String,
                footerText = map["footerText"] as String,
                inputTray = map["inputTray"] as String,
                networkTimeoutSeconds = (map["networkTimeoutSeconds"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(305) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(printerId)
        writeInt(paperSize.nativeValue)
        writeDouble(orientationDegrees)
        writeInt(quality.nativeValue)
        writeInt(copies.toLong())
        writeBool(collate)
        writeBool(duplex)
        writeBool(color)
        writeDouble(marginTop)
        writeDouble(marginBottom)
        writeDouble(marginLeft)
        writeDouble(marginRight)
        writeString(jobName)
        writeInt(pagesPerSheet.toLong())
        writeBool(showPrintDialog)
        writeInt(pageRangeFrom.toLong())
        writeInt(pageRangeTo.toLong())
        writeDouble(customPaperWidth)
        writeDouble(customPaperHeight)
        writeBool(fitToPage)
        writeInt(mediaType.nativeValue)
        writeString(headerText)
        writeString(footerText)
        writeString(inputTray)
        writeInt(networkTimeoutSeconds.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, PrintSettings> support ---
    fun toJson(): Map<String, Any?> = mapOf("printerId" to printerId, "paperSize" to paperSize.nativeValue, "orientationDegrees" to orientationDegrees, "quality" to quality.nativeValue, "copies" to copies, "collate" to collate, "duplex" to duplex, "color" to color, "marginTop" to marginTop, "marginBottom" to marginBottom, "marginLeft" to marginLeft, "marginRight" to marginRight, "jobName" to jobName, "pagesPerSheet" to pagesPerSheet, "showPrintDialog" to showPrintDialog, "pageRangeFrom" to pageRangeFrom, "pageRangeTo" to pageRangeTo, "customPaperWidth" to customPaperWidth, "customPaperHeight" to customPaperHeight, "fitToPage" to fitToPage, "mediaType" to mediaType.nativeValue, "headerText" to headerText, "footerText" to footerText, "inputTray" to inputTray, "networkTimeoutSeconds" to networkTimeoutSeconds)

}

@androidx.annotation.Keep
data class PrintDocument(val id: String, val title: String, val type: DocumentType, val data: ByteArray) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrintDocument {
            val id = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val title = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val type = DocumentType.fromNative(buf.long)
            val data = { val _len = buf.int; val _b = ByteArray(_len); buf.get(_b); _b }()
            return PrintDocument(id, title, type, data)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrintDocument {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): PrintDocument = PrintDocument(
                id = map["id"] as String,
                title = map["title"] as String,
                type = DocumentType.fromNative(map["type"] as Long),
                data = @Suppress("UNCHECKED_CAST") (map["data"] as ByteArray)
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(148) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(id)
        writeString(title)
        writeInt(type.nativeValue)
        run { writeInt32(data.size); out.write(data) }
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, PrintDocument> support ---
    fun toJson(): Map<String, Any?> = mapOf("id" to id, "title" to title, "type" to type.nativeValue, "data" to data)

}

@androidx.annotation.Keep
data class PrintJob(val id: String, val printerId: String, val documentTitle: String, val state: PrintState, val progress: Long, val createdAtMillis: Long, val completedAtMillis: Long, val errorMessage: String, val pagesPrinted: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrintJob {
            val id = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val printerId = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val documentTitle = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val state = PrintState.fromNative(buf.long)
            val progress = buf.long
            val createdAtMillis = buf.long
            val completedAtMillis = buf.long
            val errorMessage = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val pagesPrinted = buf.long
            return PrintJob(id, printerId, documentTitle, state, progress, createdAtMillis, completedAtMillis, errorMessage, pagesPrinted)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrintJob {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): PrintJob = PrintJob(
                id = map["id"] as String,
                printerId = map["printerId"] as String,
                documentTitle = map["documentTitle"] as String,
                state = PrintState.fromNative(map["state"] as Long),
                progress = (map["progress"] as Number).toLong(),
                createdAtMillis = (map["createdAtMillis"] as Number).toLong(),
                completedAtMillis = (map["completedAtMillis"] as Number).toLong(),
                errorMessage = map["errorMessage"] as String,
                pagesPrinted = (map["pagesPrinted"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(184) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(id)
        writeString(printerId)
        writeString(documentTitle)
        writeInt(state.nativeValue)
        writeInt(progress.toLong())
        writeInt(createdAtMillis.toLong())
        writeInt(completedAtMillis.toLong())
        writeString(errorMessage)
        writeInt(pagesPrinted.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, PrintJob> support ---
    fun toJson(): Map<String, Any?> = mapOf("id" to id, "printerId" to printerId, "documentTitle" to documentTitle, "state" to state.nativeValue, "progress" to progress, "createdAtMillis" to createdAtMillis, "completedAtMillis" to completedAtMillis, "errorMessage" to errorMessage, "pagesPrinted" to pagesPrinted)

}

@androidx.annotation.Keep
data class PrintResult(val success: Boolean, val jobId: String, val errorMessage: String, val errorCode: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrintResult {
            val success = (buf.get().toInt() != 0)
            val jobId = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val errorMessage = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val errorCode = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return PrintResult(success, jobId, errorMessage, errorCode)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrintResult {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): PrintResult = PrintResult(
                success = map["success"] as Boolean,
                jobId = map["jobId"] as String,
                errorMessage = map["errorMessage"] as String,
                errorCode = map["errorCode"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(109) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(success)
        writeString(jobId)
        writeString(errorMessage)
        writeString(errorCode)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, PrintResult> support ---
    fun toJson(): Map<String, Any?> = mapOf("success" to success, "jobId" to jobId, "errorMessage" to errorMessage, "errorCode" to errorCode)

}

@androidx.annotation.Keep
data class PrintDialogResult(val confirmed: Boolean, val confirmedSettings: PrintSettings, val errorMessage: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrintDialogResult {
            val confirmed = (buf.get().toInt() != 0)
            val confirmedSettings = PrintSettings.decodeFrom(buf)
            val errorMessage = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return PrintDialogResult(confirmed, confirmedSettings, errorMessage)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrintDialogResult {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): PrintDialogResult = PrintDialogResult(
                confirmed = map["confirmed"] as Boolean,
                confirmedSettings = PrintSettings.fromJson(@Suppress("UNCHECKED_CAST") (map["confirmedSettings"] as Map<String, Any?>)),
                errorMessage = map["errorMessage"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(69) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(confirmed)
        confirmedSettings.writeFieldsTo(out, buf)
        writeString(errorMessage)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, PrintDialogResult> support ---
    fun toJson(): Map<String, Any?> = mapOf("confirmed" to confirmed, "confirmedSettings" to confirmedSettings.toJson(), "errorMessage" to errorMessage)

}

@androidx.annotation.Keep
data class PrintJobUpdate(val jobId: String, val state: PrintState, val progress: Long, val message: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrintJobUpdate {
            val jobId = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val state = PrintState.fromNative(buf.long)
            val progress = buf.long
            val message = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return PrintJobUpdate(jobId, state, progress, message)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrintJobUpdate {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): PrintJobUpdate = PrintJobUpdate(
                jobId = map["jobId"] as String,
                state = PrintState.fromNative(map["state"] as Long),
                progress = (map["progress"] as Number).toLong(),
                message = map["message"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(88) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(jobId)
        writeInt(state.nativeValue)
        writeInt(progress.toLong())
        writeString(message)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, PrintJobUpdate> support ---
    fun toJson(): Map<String, Any?> = mapOf("jobId" to jobId, "state" to state.nativeValue, "progress" to progress, "message" to message)

}

@androidx.annotation.Keep
data class PrinterStatus(val printerId: String, val isOnline: Boolean, val isPrinting: Boolean, val jobsInQueue: Long, val statusMessage: String, val inkLevel: Long, val tonerLevel: Long, val paperLevel: Long, val errorCode: String, val isWarmingUp: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrinterStatus {
            val printerId = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val isOnline = (buf.get().toInt() != 0)
            val isPrinting = (buf.get().toInt() != 0)
            val jobsInQueue = buf.long
            val statusMessage = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val inkLevel = buf.long
            val tonerLevel = buf.long
            val paperLevel = buf.long
            val errorCode = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val isWarmingUp = (buf.get().toInt() != 0)
            return PrinterStatus(printerId, isOnline, isPrinting, jobsInQueue, statusMessage, inkLevel, tonerLevel, paperLevel, errorCode, isWarmingUp)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrinterStatus {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): PrinterStatus = PrinterStatus(
                printerId = map["printerId"] as String,
                isOnline = map["isOnline"] as Boolean,
                isPrinting = map["isPrinting"] as Boolean,
                jobsInQueue = (map["jobsInQueue"] as Number).toLong(),
                statusMessage = map["statusMessage"] as String,
                inkLevel = (map["inkLevel"] as Number).toLong(),
                tonerLevel = (map["tonerLevel"] as Number).toLong(),
                paperLevel = (map["paperLevel"] as Number).toLong(),
                errorCode = map["errorCode"] as String,
                isWarmingUp = map["isWarmingUp"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(143) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(printerId)
        writeBool(isOnline)
        writeBool(isPrinting)
        writeInt(jobsInQueue.toLong())
        writeString(statusMessage)
        writeInt(inkLevel.toLong())
        writeInt(tonerLevel.toLong())
        writeInt(paperLevel.toLong())
        writeString(errorCode)
        writeBool(isWarmingUp)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, PrinterStatus> support ---
    fun toJson(): Map<String, Any?> = mapOf("printerId" to printerId, "isOnline" to isOnline, "isPrinting" to isPrinting, "jobsInQueue" to jobsInQueue, "statusMessage" to statusMessage, "inkLevel" to inkLevel, "tonerLevel" to tonerLevel, "paperLevel" to paperLevel, "errorCode" to errorCode, "isWarmingUp" to isWarmingUp)

}

@androidx.annotation.Keep
data class DiscoveredPrinter(val id: String, val name: String, val host: String, val port: Long, val serviceType: String, val uri: String, val isAvailable: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): DiscoveredPrinter {
            val id = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val name = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val host = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val port = buf.long
            val serviceType = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val uri = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val isAvailable = (buf.get().toInt() != 0)
            return DiscoveredPrinter(id, name, host, port, serviceType, uri, isAvailable)
        }
        @JvmStatic fun decode(bytes: ByteArray): DiscoveredPrinter {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): DiscoveredPrinter = DiscoveredPrinter(
                id = map["id"] as String,
                name = map["name"] as String,
                host = map["host"] as String,
                port = (map["port"] as Number).toLong(),
                serviceType = map["serviceType"] as String,
                uri = map["uri"] as String,
                isAvailable = map["isAvailable"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(189) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(id)
        writeString(name)
        writeString(host)
        writeInt(port.toLong())
        writeString(serviceType)
        writeString(uri)
        writeBool(isAvailable)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, DiscoveredPrinter> support ---
    fun toJson(): Map<String, Any?> = mapOf("id" to id, "name" to name, "host" to host, "port" to port, "serviceType" to serviceType, "uri" to uri, "isAvailable" to isAvailable)

}

@androidx.annotation.Keep
data class PrinterStatusDetail(val printerId: String, val isOnline: Boolean, val isReady: Boolean, val hasPaperJam: Boolean, val isOutOfPaper: Boolean, val isOutOfInk: Boolean, val inkLevelBlack: Long, val inkLevelCyan: Long, val inkLevelMagenta: Long, val inkLevelYellow: Long, val tonerLevel: Long, val paperLevel: Long, val jobsInQueue: Long, val isWarmingUp: Boolean, val printerState: String, val stateReasons: String, val statusMessage: String, val errorCode: String, val isDuplexSupported: Boolean, val isColorSupported: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PrinterStatusDetail {
            val printerId = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val isOnline = (buf.get().toInt() != 0)
            val isReady = (buf.get().toInt() != 0)
            val hasPaperJam = (buf.get().toInt() != 0)
            val isOutOfPaper = (buf.get().toInt() != 0)
            val isOutOfInk = (buf.get().toInt() != 0)
            val inkLevelBlack = buf.long
            val inkLevelCyan = buf.long
            val inkLevelMagenta = buf.long
            val inkLevelYellow = buf.long
            val tonerLevel = buf.long
            val paperLevel = buf.long
            val jobsInQueue = buf.long
            val isWarmingUp = (buf.get().toInt() != 0)
            val printerState = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val stateReasons = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val statusMessage = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val errorCode = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val isDuplexSupported = (buf.get().toInt() != 0)
            val isColorSupported = (buf.get().toInt() != 0)
            return PrinterStatusDetail(printerId, isOnline, isReady, hasPaperJam, isOutOfPaper, isOutOfInk, inkLevelBlack, inkLevelCyan, inkLevelMagenta, inkLevelYellow, tonerLevel, paperLevel, jobsInQueue, isWarmingUp, printerState, stateReasons, statusMessage, errorCode, isDuplexSupported, isColorSupported)
        }
        @JvmStatic fun decode(bytes: ByteArray): PrinterStatusDetail {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): PrinterStatusDetail = PrinterStatusDetail(
                printerId = map["printerId"] as String,
                isOnline = map["isOnline"] as Boolean,
                isReady = map["isReady"] as Boolean,
                hasPaperJam = map["hasPaperJam"] as Boolean,
                isOutOfPaper = map["isOutOfPaper"] as Boolean,
                isOutOfInk = map["isOutOfInk"] as Boolean,
                inkLevelBlack = (map["inkLevelBlack"] as Number).toLong(),
                inkLevelCyan = (map["inkLevelCyan"] as Number).toLong(),
                inkLevelMagenta = (map["inkLevelMagenta"] as Number).toLong(),
                inkLevelYellow = (map["inkLevelYellow"] as Number).toLong(),
                tonerLevel = (map["tonerLevel"] as Number).toLong(),
                paperLevel = (map["paperLevel"] as Number).toLong(),
                jobsInQueue = (map["jobsInQueue"] as Number).toLong(),
                isWarmingUp = map["isWarmingUp"] as Boolean,
                printerState = map["printerState"] as String,
                stateReasons = map["stateReasons"] as String,
                statusMessage = map["statusMessage"] as String,
                errorCode = map["errorCode"] as String,
                isDuplexSupported = map["isDuplexSupported"] as Boolean,
                isColorSupported = map["isColorSupported"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(244) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(printerId)
        writeBool(isOnline)
        writeBool(isReady)
        writeBool(hasPaperJam)
        writeBool(isOutOfPaper)
        writeBool(isOutOfInk)
        writeInt(inkLevelBlack.toLong())
        writeInt(inkLevelCyan.toLong())
        writeInt(inkLevelMagenta.toLong())
        writeInt(inkLevelYellow.toLong())
        writeInt(tonerLevel.toLong())
        writeInt(paperLevel.toLong())
        writeInt(jobsInQueue.toLong())
        writeBool(isWarmingUp)
        writeString(printerState)
        writeString(stateReasons)
        writeString(statusMessage)
        writeString(errorCode)
        writeBool(isDuplexSupported)
        writeBool(isColorSupported)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, PrinterStatusDetail> support ---
    fun toJson(): Map<String, Any?> = mapOf("printerId" to printerId, "isOnline" to isOnline, "isReady" to isReady, "hasPaperJam" to hasPaperJam, "isOutOfPaper" to isOutOfPaper, "isOutOfInk" to isOutOfInk, "inkLevelBlack" to inkLevelBlack, "inkLevelCyan" to inkLevelCyan, "inkLevelMagenta" to inkLevelMagenta, "inkLevelYellow" to inkLevelYellow, "tonerLevel" to tonerLevel, "paperLevel" to paperLevel, "jobsInQueue" to jobsInQueue, "isWarmingUp" to isWarmingUp, "printerState" to printerState, "stateReasons" to stateReasons, "statusMessage" to statusMessage, "errorCode" to errorCode, "isDuplexSupported" to isDuplexSupported, "isColorSupported" to isColorSupported)

}

@androidx.annotation.Keep
data class NitroNullableInt(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableInt {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroNullableInt(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableInt {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableInt = NitroNullableInt(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Long?) : this(v != null, v ?: 0L)
    val nullable: Long? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableInt> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroNullableDouble(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableDouble {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroNullableDouble(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableDouble {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableDouble = NitroNullableDouble(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Double?) : this(v != null, v ?: 0.0)
    val nullable: Double? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableDouble> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroNullableBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroNullableBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableBool = NitroNullableBool(
                hasValue = map["hasValue"] as Boolean,
                value = map["value"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(2) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Boolean?) : this(v != null, v ?: false)
    val nullable: Boolean? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableBool> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptInt64(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptInt64 {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroOptInt64(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptInt64 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptInt64 = NitroOptInt64(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Long?) : this(v != null, v ?: 0L)
    val nullable: Long? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptInt64> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptFloat64(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptFloat64 {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroOptFloat64(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptFloat64 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptFloat64 = NitroOptFloat64(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Double?) : this(v != null, v ?: 0.0)
    val nullable: Double? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptFloat64> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroOptBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptBool = NitroOptBool(
                hasValue = map["hasValue"] as Boolean,
                value = map["value"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(2) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Boolean?) : this(v != null, v ?: false)
    val nullable: Boolean? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptBool> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

/** @NitroResult bridge encoding helpers. */
private fun nitroWriteResultTag(tag: Byte, payload: ByteArray): ByteArray {
    val buf = ByteArray(1 + payload.size)
    buf[0] = tag
    payload.copyInto(buf, 1)
    return buf
}

private fun nitroEncodeResultInt64(v: Long): ByteArray {
    val w = java.nio.ByteBuffer.allocate(4 + 8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
    w.putInt(8); w.putLong(v)
    return nitroWriteResultTag(0, w.array())
}

private fun nitroEncodeResultFloat64(v: Double): ByteArray {
    val w = java.nio.ByteBuffer.allocate(4 + 8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
    w.putInt(8); w.putDouble(v)
    return nitroWriteResultTag(0, w.array())
}

private fun nitroEncodeResultBool(v: Boolean): ByteArray {
    val w = java.nio.ByteBuffer.allocate(4 + 1).order(java.nio.ByteOrder.LITTLE_ENDIAN)
    w.putInt(1); w.put(if (v) 1.toByte() else 0.toByte())
    return nitroWriteResultTag(0, w.array())
}

private fun nitroEncodeResultString(v: String): ByteArray {
    val bytes = v.toByteArray(Charsets.UTF_8)
    val w = java.nio.ByteBuffer.allocate(4 + 4 + bytes.size).order(java.nio.ByteOrder.LITTLE_ENDIAN)
    w.putInt(4 + bytes.size); w.putInt(bytes.size); w.put(bytes)
    return nitroWriteResultTag(0, w.array())
}

private fun nitroEncodeResultRecord(v: Any): ByteArray {
    // For @HybridRecord: encode via the generated encode() method (duck-typed via reflection)
    return try {
        val method = v.javaClass.getMethod("encode")
        val payload = method.invoke(v) as? ByteArray ?: ByteArray(4)
        nitroWriteResultTag(0, payload)
    } catch (_: Throwable) {
        nitroEncodeResultError("Record encode failed")
    }
}

private fun nitroEncodeResultError(msg: String): ByteArray {
    // Encode error as [1B tag=1][string payload]
    val errBytes = msg.toByteArray(Charsets.UTF_8)
    val w = java.nio.ByteBuffer.allocate(4 + 4 + errBytes.size).order(java.nio.ByteOrder.LITTLE_ENDIAN)
    w.putInt(4 + errBytes.size); w.putInt(errBytes.size); w.put(errBytes)
    return nitroWriteResultTag(1, w.array())
}

/**
 * Contract for the [NitroPrinting] module.
 * Implement this in your Kotlin source code.
 * Nitro may call this implementation from any JNI thread.
 * Keep mutable state thread-safe or marshal work onto your own dispatcher.
 */
interface HybridNitroPrintingSpec {
    val applicationContext: Context get() = NitroPrintingJniBridge.applicationContext
    val activity: Activity? get() = NitroPrintingJniBridge.activity

    // Optional lifecycle hooks — override only what you need.
    fun onAttached() {}
    fun onDetached() {}
    fun onActivityAttached(activity: Activity) {}
    fun onActivityDetached() {}

    // source: nitro_printing.native.dart:16
    fun isPrintingSupported(): Boolean
    // source: nitro_printing.native.dart:17
    fun getPrintersCount(): Long
    // source: nitro_printing.native.dart:18
    fun getPrinterDriverVersion(printerId: String): String
    // source: nitro_printing.native.dart:26
    suspend fun getAllPrinters(): List<PrinterInfo>
    // source: nitro_printing.native.dart:31
    suspend fun getPrinterAt(index: Long): PrinterInfo
    // source: nitro_printing.native.dart:36
    suspend fun getDefaultPrinter(): PrinterInfo
    // source: nitro_printing.native.dart:41
    suspend fun getPrinterCapabilities(printerId: String): PrinterCapabilities
    // source: nitro_printing.native.dart:48
    suspend fun printText(text: String, settings: PrintSettings?): PrintResult
    // source: nitro_printing.native.dart:51
    suspend fun printImage(imageData: ByteArray, settings: PrintSettings?): PrintResult
    // source: nitro_printing.native.dart:57
    suspend fun printPdf(pdfData: ByteArray, settings: PrintSettings?): PrintResult
    // source: nitro_printing.native.dart:60
    suspend fun printDocument(document: PrintDocument, settings: PrintSettings?): PrintResult
    // source: nitro_printing.native.dart:66
    suspend fun printFile(filePath: String, settings: PrintSettings?): Boolean
    // source: nitro_printing.native.dart:74
    suspend fun printBatch(documents: List<PrintDocument>, stopOnError: Boolean, settings: PrintSettings?): List<PrintResult>
    // source: nitro_printing.native.dart:91
    suspend fun showPrintDialog(document: PrintDocument, initialSettings: PrintSettings?): PrintDialogResult
    // source: nitro_printing.native.dart:100
    suspend fun renderPreview(document: PrintDocument, settings: PrintSettings?): PreviewResult
    // source: nitro_printing.native.dart:107
    suspend fun getPageCount(document: PrintDocument): Long
    // source: nitro_printing.native.dart:111
    suspend fun printToFile(document: PrintDocument, outputPath: String, settings: PrintSettings?): Boolean
    // source: nitro_printing.native.dart:120
    suspend fun cancelPrintJob(jobId: String): Boolean
    // source: nitro_printing.native.dart:123
    suspend fun pausePrintJob(jobId: String): Boolean
    // source: nitro_printing.native.dart:126
    suspend fun resumePrintJob(jobId: String): Boolean
    // source: nitro_printing.native.dart:129
    suspend fun clearPrintQueue(): Boolean
    // source: nitro_printing.native.dart:132
    suspend fun getPrintJobsCount(): Long
    // source: nitro_printing.native.dart:137
    suspend fun getPrintJobAt(index: Long): PrintJob
    // source: nitro_printing.native.dart:142
    suspend fun getPrintJobStatus(jobId: String): PrintJob
    // source: nitro_printing.native.dart:148
    suspend fun startPrinterDiscovery(): Boolean
    // source: nitro_printing.native.dart:151
    suspend fun stopPrinterDiscovery(): Boolean
    // source: nitro_printing.native.dart:157
    suspend fun testPrinterConnection(printerId: String, timeoutSeconds: Long?): Boolean
    // source: nitro_printing.native.dart:161
    suspend fun setDefaultPrinter(printerId: String): Boolean
    // source: nitro_printing.native.dart:167
    suspend fun openSystemPrintQueue(printerId: String): Boolean
    // source: nitro_printing.native.dart:171
    suspend fun openPrinterProperties(printerId: String): Boolean
    // source: nitro_printing.native.dart:177
    suspend fun printRaw(data: ByteArray, settings: PrintSettings?): PrintResult
    // source: nitro_printing.native.dart:181
    suspend fun printEscPos(escPosData: ByteArray, settings: PrintSettings?): PrintResult
    // source: nitro_printing.native.dart:188
    suspend fun printZpl(zpl: String, settings: PrintSettings?): PrintResult
    // source: nitro_printing.native.dart:192
    suspend fun cancelRawPrint(): Boolean
    // source: nitro_printing.native.dart:200
    suspend fun getPrinterStatusDetail(printerId: String, timeoutSeconds: Long?): PrinterStatusDetail
    val onPrintJobChanged: Flow<PrintJobUpdate>
    val onPrinterStatusChanged: Flow<PrinterStatus>
    val onPrinterDiscovered: Flow<DiscoveredPrinter>
}

@Keep
object NitroPrintingJniBridge {
    private val _implementations = java.util.concurrent.ConcurrentHashMap<Long, HybridNitroPrintingSpec>()
    private val _idCounter = java.util.concurrent.atomic.AtomicLong(0)
    private var _factory: (() -> HybridNitroPrintingSpec)? = null
    private val _asyncExecutor = java.util.concurrent.Executors.newCachedThreadPool()

    lateinit var applicationContext: Context
        private set

    var activity: Activity? = null
        private set

    @JvmStatic external fun initialize(bridgeClass: Class<*>)

    fun registerFactory(factory: () -> HybridNitroPrintingSpec, context: Context) {
        applicationContext = context
        _factory = factory
        if (_implementations.isEmpty()) { initialize(this::class.java) }
    }

    @JvmStatic
    fun create_instance_call(key: String): Long {
        val factory = _factory ?: throw IllegalStateException(
            "NitroPrinting: no factory registered. Call registerFactory() in onAttachedToEngine().")
        val id = _idCounter.getAndIncrement()
        val impl = factory()
        _implementations[id] = impl
        impl.onAttached()
        return id
    }

    @JvmStatic
    fun destroy_instance_call(instanceId: Long) {
        _implementations.remove(instanceId)?.onDetached()
    }

    fun onActivityAttached(newActivity: Activity) {
        activity = newActivity
        _implementations.values.forEach { it.onActivityAttached(newActivity) }
    }

    fun onActivityDetached() {
        activity = null
        _implementations.values.forEach { it.onActivityDetached() }
    }

    // @NitroNativeAsync helpers — post primitive results via Dart_PostCObject_DL.
    @JvmStatic external fun postNullToPort(dartPort: Long)
    @JvmStatic external fun postInt64ToPort(dartPort: Long, value: Long)
    @JvmStatic external fun postDoubleToPort(dartPort: Long, value: Double)
    @JvmStatic external fun postBoolToPort(dartPort: Long, value: Boolean)
    @JvmStatic external fun postStringToPort(dartPort: Long, value: String)
    // Nullable prim helpers: malloc NitroOptXxx on native heap, post address as kInt64.
    // Dart decodes via Pointer<NitroOptXxx>.fromAddress and frees via the <lib>_nitro_free export.
    @JvmStatic external fun postOptInt64ToPort(dartPort: Long, value: Long, hasValue: Boolean)
    @JvmStatic external fun postOptFloat64ToPort(dartPort: Long, value: Double, hasValue: Boolean)
    @JvmStatic external fun postOptBoolToPort(dartPort: Long, value: Boolean, hasValue: Boolean)
    // Record/list-record helper: malloc a [4B len][payload] native buffer, copy
    // value into it, post the address as kInt64. Dart decodes via Pointer<Uint8>.
    // null posts address 0 (nullptr) — NOT Dart_CObject_kNull — matching how
    // nullable record returns are decoded elsewhere (a pointer-typed kInt64,
    // never a bare kNull CObject).
    @JvmStatic external fun postBytesToPort(dartPort: Long, value: ByteArray?)
    // PreviewResult NativeAsync return: malloc a native copy via pack_PreviewResult_from_jni, post
    // address as kInt64. null posts address 0 — NOT Dart_CObject_kNull — same
    // convention as postBytesToPort above.
    @JvmStatic external fun postPreviewResultToPort(dartPort: Long, value: PreviewResult?)
    // Writes a thrown exception's name/message into the fresh-per-call
    // NitroError* whose address Dart passed as errPtr, before the trampoline
    // still posts exactly one message so the port fires either way.
    @JvmStatic external fun reportNativeAsyncError(errPtr: Long, name: String, message: String)

    // source: nitro_printing.native.dart:16
    @JvmStatic fun isPrintingSupported_call(instanceId: Long): Boolean {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroPrinting instance $instanceId not registered")
        return impl.isPrintingSupported()
    }
    // source: nitro_printing.native.dart:17
    @JvmStatic fun getPrintersCount_call(instanceId: Long): Long {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroPrinting instance $instanceId not registered")
        return impl.getPrintersCount()
    }
    // source: nitro_printing.native.dart:18
    @JvmStatic fun getPrinterDriverVersion_call(instanceId: Long, printerId: String): String {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroPrinting instance $instanceId not registered")
        return impl.getPrinterDriverVersion(printerId)
    }
    // source: nitro_printing.native.dart:26
    @JvmStatic fun getAllPrinters_call(instanceId: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.getAllPrinters() }
            val itemBufs = ArrayList<ByteArray>(result.size)
            val tmpBuf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            for (item in result) {
                val tmpOut = java.io.ByteArrayOutputStream(110)
                item.writeFieldsTo(tmpOut, tmpBuf)
                itemBufs.add(tmpOut.toByteArray())
            }
            // payload = [4B count][8B × n offsets][item bytes...]
            var offsetPos = 4 + 8L * result.size  // start of item data in payload
            val offsets = LongArray(result.size)
            for (i in result.indices) { offsets[i] = offsetPos; offsetPos += itemBufs[i].size }
            val payloadBuf = java.nio.ByteBuffer.allocate(offsetPos.toInt()).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            payloadBuf.putInt(result.size)
            offsets.forEach { payloadBuf.putLong(it) }
            itemBufs.forEach { payloadBuf.put(it) }
            val payload = payloadBuf.array()
            val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            lenBuf.putInt(payload.size)
            val _bytes = lenBuf.array() + payload
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:31
    @JvmStatic fun getPrinterAt_call(instanceId: Long, index: Long): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroPrinting instance $instanceId not registered")
        return try {
            val _result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.getPrinterAt(index) } }).get()
            nitroEncodeResultRecord(_result)
        } catch (_e: Throwable) {
            nitroEncodeResultError(_e.message ?: "Unknown error")
        }
    }
    // source: nitro_printing.native.dart:36
    @JvmStatic fun getDefaultPrinter_call(instanceId: Long): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroPrinting instance $instanceId not registered")
        return try {
            val _result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.getDefaultPrinter() } }).get()
            nitroEncodeResultRecord(_result)
        } catch (_e: Throwable) {
            nitroEncodeResultError(_e.message ?: "Unknown error")
        }
    }
    // source: nitro_printing.native.dart:41
    @JvmStatic fun getPrinterCapabilities_call(instanceId: Long, printerId: String): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroPrinting instance $instanceId not registered")
        return try {
            val _result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.getPrinterCapabilities(printerId) } }).get()
            nitroEncodeResultRecord(_result)
        } catch (_e: Throwable) {
            nitroEncodeResultError(_e.message ?: "Unknown error")
        }
    }
    // source: nitro_printing.native.dart:48
    @JvmStatic fun printText_call(instanceId: Long, text: String, settings: ByteArray?, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val settingsDecoded: PrintSettings? = if (settings == null) null else {
            val _buf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            _buf.getInt() // skip 4-byte prefix
            PrintSettings.decodeFrom(_buf)
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.printText(text, settingsDecoded) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:51
    @JvmStatic fun printImage_call(instanceId: Long, imageData: ByteArray, settings: ByteArray?, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val settingsDecoded: PrintSettings? = if (settings == null) null else {
            val _buf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            _buf.getInt() // skip 4-byte prefix
            PrintSettings.decodeFrom(_buf)
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.printImage(imageData, settingsDecoded) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:57
    @JvmStatic fun printPdf_call(instanceId: Long, pdfData: ByteArray, settings: ByteArray?, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val settingsDecoded: PrintSettings? = if (settings == null) null else {
            val _buf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            _buf.getInt() // skip 4-byte prefix
            PrintSettings.decodeFrom(_buf)
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.printPdf(pdfData, settingsDecoded) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:60
    @JvmStatic fun printDocument_call(instanceId: Long, document: ByteArray, settings: ByteArray?, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val documentBuf = java.nio.ByteBuffer.wrap(document).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        documentBuf.getInt() // skip Dart 4-byte outer length prefix
        val documentDecoded = PrintDocument.decodeFrom(documentBuf)
        val settingsDecoded: PrintSettings? = if (settings == null) null else {
            val _buf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            _buf.getInt() // skip 4-byte prefix
            PrintSettings.decodeFrom(_buf)
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.printDocument(documentDecoded, settingsDecoded) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:66
    @JvmStatic fun printFile_call(instanceId: Long, filePath: String, settings: ByteArray?, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val settingsDecoded: PrintSettings? = if (settings == null) null else {
            val _buf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            _buf.getInt() // skip 4-byte prefix
            PrintSettings.decodeFrom(_buf)
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.printFile(filePath, settingsDecoded) }
            postBoolToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:74
    @JvmStatic fun printBatch_call(instanceId: Long, documents: ByteArray, stopOnError: Boolean, settings: ByteArray?, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val documentsBuf = java.nio.ByteBuffer.wrap(documents).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        documentsBuf.getInt() // skip outer length
        val documentsCount = documentsBuf.getInt()
        val documentsOffsets = LongArray(documentsCount) { documentsBuf.getLong() }
        val documentsDecoded = mutableListOf<PrintDocument>()
        for (documentsOffset in documentsOffsets) {
            val itemBuf = java.nio.ByteBuffer.wrap(documents, 4 + documentsOffset.toInt(), documents.size - 4 - documentsOffset.toInt()).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            documentsDecoded.add(PrintDocument.decodeFrom(itemBuf))
        }
        val settingsDecoded: PrintSettings? = if (settings == null) null else {
            val _buf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            _buf.getInt() // skip 4-byte prefix
            PrintSettings.decodeFrom(_buf)
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.printBatch(documentsDecoded, stopOnError, settingsDecoded) }
            val itemBufs = ArrayList<ByteArray>(result.size)
            val tmpBuf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            for (item in result) {
                val tmpOut = java.io.ByteArrayOutputStream(109)
                item.writeFieldsTo(tmpOut, tmpBuf)
                itemBufs.add(tmpOut.toByteArray())
            }
            // payload = [4B count][8B × n offsets][item bytes...]
            var offsetPos = 4 + 8L * result.size  // start of item data in payload
            val offsets = LongArray(result.size)
            for (i in result.indices) { offsets[i] = offsetPos; offsetPos += itemBufs[i].size }
            val payloadBuf = java.nio.ByteBuffer.allocate(offsetPos.toInt()).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            payloadBuf.putInt(result.size)
            offsets.forEach { payloadBuf.putLong(it) }
            itemBufs.forEach { payloadBuf.put(it) }
            val payload = payloadBuf.array()
            val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            lenBuf.putInt(payload.size)
            val _bytes = lenBuf.array() + payload
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:91
    @JvmStatic fun showPrintDialog_call(instanceId: Long, document: ByteArray, initialSettings: ByteArray?, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val documentBuf = java.nio.ByteBuffer.wrap(document).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        documentBuf.getInt() // skip Dart 4-byte outer length prefix
        val documentDecoded = PrintDocument.decodeFrom(documentBuf)
        val initialSettingsDecoded: PrintSettings? = if (initialSettings == null) null else {
            val _buf = java.nio.ByteBuffer.wrap(initialSettings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            _buf.getInt() // skip 4-byte prefix
            PrintSettings.decodeFrom(_buf)
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.showPrintDialog(documentDecoded, initialSettingsDecoded) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:100
    @JvmStatic fun renderPreview_call(instanceId: Long, document: ByteArray, settings: ByteArray?, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val documentBuf = java.nio.ByteBuffer.wrap(document).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        documentBuf.getInt() // skip Dart 4-byte outer length prefix
        val documentDecoded = PrintDocument.decodeFrom(documentBuf)
        val settingsDecoded: PrintSettings? = if (settings == null) null else {
            val _buf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            _buf.getInt() // skip 4-byte prefix
            PrintSettings.decodeFrom(_buf)
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.renderPreview(documentDecoded, settingsDecoded) }
            postPreviewResultToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:107
    @JvmStatic fun getPageCount_call(instanceId: Long, document: ByteArray, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val documentBuf = java.nio.ByteBuffer.wrap(document).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        documentBuf.getInt() // skip Dart 4-byte outer length prefix
        val documentDecoded = PrintDocument.decodeFrom(documentBuf)
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.getPageCount(documentDecoded) }
            postInt64ToPort(dartPort, result.toLong())
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:111
    @JvmStatic fun printToFile_call(instanceId: Long, document: ByteArray, outputPath: String, settings: ByteArray?, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val documentBuf = java.nio.ByteBuffer.wrap(document).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        documentBuf.getInt() // skip Dart 4-byte outer length prefix
        val documentDecoded = PrintDocument.decodeFrom(documentBuf)
        val settingsDecoded: PrintSettings? = if (settings == null) null else {
            val _buf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            _buf.getInt() // skip 4-byte prefix
            PrintSettings.decodeFrom(_buf)
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.printToFile(documentDecoded, outputPath, settingsDecoded) }
            postBoolToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:120
    @JvmStatic fun cancelPrintJob_call(instanceId: Long, jobId: String, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.cancelPrintJob(jobId) }
            postBoolToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:123
    @JvmStatic fun pausePrintJob_call(instanceId: Long, jobId: String, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.pausePrintJob(jobId) }
            postBoolToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:126
    @JvmStatic fun resumePrintJob_call(instanceId: Long, jobId: String, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.resumePrintJob(jobId) }
            postBoolToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:129
    @JvmStatic fun clearPrintQueue_call(instanceId: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.clearPrintQueue() }
            postBoolToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:132
    @JvmStatic fun getPrintJobsCount_call(instanceId: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.getPrintJobsCount() }
            postInt64ToPort(dartPort, result.toLong())
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:137
    @JvmStatic fun getPrintJobAt_call(instanceId: Long, index: Long): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroPrinting instance $instanceId not registered")
        return try {
            val _result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.getPrintJobAt(index) } }).get()
            nitroEncodeResultRecord(_result)
        } catch (_e: Throwable) {
            nitroEncodeResultError(_e.message ?: "Unknown error")
        }
    }
    // source: nitro_printing.native.dart:142
    @JvmStatic fun getPrintJobStatus_call(instanceId: Long, jobId: String): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroPrinting instance $instanceId not registered")
        return try {
            val _result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.getPrintJobStatus(jobId) } }).get()
            nitroEncodeResultRecord(_result)
        } catch (_e: Throwable) {
            nitroEncodeResultError(_e.message ?: "Unknown error")
        }
    }
    // source: nitro_printing.native.dart:148
    @JvmStatic fun startPrinterDiscovery_call(instanceId: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.startPrinterDiscovery() }
            postBoolToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:151
    @JvmStatic fun stopPrinterDiscovery_call(instanceId: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.stopPrinterDiscovery() }
            postBoolToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:157
    @JvmStatic fun testPrinterConnection_call(instanceId: Long, printerId: String, timeoutSeconds: ByteArray, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val timeoutSecondsArg: Long? = NitroOptInt64.decode(timeoutSeconds).nullable
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.testPrinterConnection(printerId, timeoutSecondsArg) }
            postBoolToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:161
    @JvmStatic fun setDefaultPrinter_call(instanceId: Long, printerId: String, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.setDefaultPrinter(printerId) }
            postBoolToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:167
    @JvmStatic fun openSystemPrintQueue_call(instanceId: Long, printerId: String, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.openSystemPrintQueue(printerId) }
            postBoolToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:171
    @JvmStatic fun openPrinterProperties_call(instanceId: Long, printerId: String, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.openPrinterProperties(printerId) }
            postBoolToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:177
    @JvmStatic fun printRaw_call(instanceId: Long, data: ByteArray, settings: ByteArray?, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val settingsDecoded: PrintSettings? = if (settings == null) null else {
            val _buf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            _buf.getInt() // skip 4-byte prefix
            PrintSettings.decodeFrom(_buf)
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.printRaw(data, settingsDecoded) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:181
    @JvmStatic fun printEscPos_call(instanceId: Long, escPosData: ByteArray, settings: ByteArray?, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val settingsDecoded: PrintSettings? = if (settings == null) null else {
            val _buf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            _buf.getInt() // skip 4-byte prefix
            PrintSettings.decodeFrom(_buf)
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.printEscPos(escPosData, settingsDecoded) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:188
    @JvmStatic fun printZpl_call(instanceId: Long, zpl: String, settings: ByteArray?, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val settingsDecoded: PrintSettings? = if (settings == null) null else {
            val _buf = java.nio.ByteBuffer.wrap(settings).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            _buf.getInt() // skip 4-byte prefix
            PrintSettings.decodeFrom(_buf)
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.printZpl(zpl, settingsDecoded) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:192
    @JvmStatic fun cancelRawPrint_call(instanceId: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.cancelRawPrint() }
            postBoolToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_printing.native.dart:200
    @JvmStatic fun getPrinterStatusDetail_call(instanceId: Long, printerId: String, timeoutSeconds: ByteArray): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroPrinting instance $instanceId not registered")
        val timeoutSecondsArg: Long? = NitroOptInt64.decode(timeoutSeconds).nullable
        return try {
            val _result = _asyncExecutor.submit(java.util.concurrent.Callable { runBlocking { impl.getPrinterStatusDetail(printerId, timeoutSecondsArg) } }).get()
            nitroEncodeResultRecord(_result)
        } catch (_e: Throwable) {
            nitroEncodeResultError(_e.message ?: "Unknown error")
        }
    }
    private val _streamJobs = java.util.concurrent.ConcurrentHashMap<Pair<String, Long>, kotlinx.coroutines.Job>()

    @JvmStatic external fun emit_onPrintJobChanged(dartPort: Long, item: ByteArray): Boolean

    @JvmStatic fun nitro_printing_register_on_print_job_changed_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("onPrintJobChanged", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.onPrintJobChanged.collect { item -> 
                if (!emit_onPrintJobChanged(dartPort, item.encode())) {
                    _streamJobs.remove(Pair("onPrintJobChanged", dartPort))?.cancel()
                    return@collect
                }
            }
        }
    }
    @JvmStatic fun nitro_printing_release_on_print_job_changed_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("onPrintJobChanged", dartPort))?.cancel()
    }
    @JvmStatic external fun emit_onPrinterStatusChanged(dartPort: Long, item: ByteArray): Boolean

    @JvmStatic fun nitro_printing_register_on_printer_status_changed_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("onPrinterStatusChanged", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.onPrinterStatusChanged.collect { item -> 
                if (!emit_onPrinterStatusChanged(dartPort, item.encode())) {
                    _streamJobs.remove(Pair("onPrinterStatusChanged", dartPort))?.cancel()
                    return@collect
                }
            }
        }
    }
    @JvmStatic fun nitro_printing_release_on_printer_status_changed_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("onPrinterStatusChanged", dartPort))?.cancel()
    }
    @JvmStatic external fun emit_onPrinterDiscovered(dartPort: Long, item: ByteArray): Boolean

    @JvmStatic fun nitro_printing_register_on_printer_discovered_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("onPrinterDiscovered", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.onPrinterDiscovered.collect { item -> 
                if (!emit_onPrinterDiscovered(dartPort, item.encode())) {
                    _streamJobs.remove(Pair("onPrinterDiscovered", dartPort))?.cancel()
                    return@collect
                }
            }
        }
    }
    @JvmStatic fun nitro_printing_release_on_printer_discovered_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("onPrinterDiscovered", dartPort))?.cancel()
    }
}
