// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.7.1
// Generated from: nitro_printing.native.dart
// ignore_for_file: non_constant_identifier_names, unnecessary_cast, unused_element, unused_field, unused_import, no_leading_underscores_for_local_identifiers, unused_local_variable
/// Web (WASM/js_interop) implementation of [NitroPrinting]. Reached through
/// the platform shim; never compiled into a native build.
library;

import 'dart:async';
import 'dart:js_interop';

// The web-bridge barrel: always resolves to the WEB runtime (the main
// nitro.dart barrel is conditional and resolves native under the analyzer).
import 'package:nitro/web_bridge.dart';

// The spec library: abstract class + data types + pure codecs (.g.dart part).
import '../../nitro_printing.native.dart';

const String _libName = 'nitro_printing';
// The default asset URL is assets/packages/<package>/assets/web/<lib>.js
// (see NitroRuntime.loadWebModule).
const String _assetPackage = 'nitro_printing';

NitroWasmModule get _module => NitroRuntime.webModule(_libName);

/// Loads and instantiates the nitro_printing WASM module. MUST be awaited before
/// [createNitroPrintingInstance] on web. Idempotent. [jsUrl] overrides the
/// default asset URL.
Future<void> ensureNitroPrintingReady({String? jsUrl}) =>
    NitroRuntime.loadWebModule(
      _libName,
      jsUrl: jsUrl,
      assetPackage: _assetPackage,
    ).then((_) {});

/// Creates the web (WASM) implementation of [NitroPrinting]. A distinct [key]
/// creates an independent native instance; the default key returns the
/// shared singleton. Await [ensureNitroPrintingReady] first.
NitroPrinting createNitroPrintingInstance([String key = 'default']) =>
    _NitroPrintingWebImpl(key);

// ── Wire helpers (web) ──────────────────────────────────────────────────

Uint8List _nitroEncodeFramed(void Function(RecordWriter w) write) {
  final w = RecordWriter();
  write(w);
  return w.takeFramedBytes();
}

Uint8List _nitroPackStructPreviewResult(
  NitroWasmModule m,
  WasmArena arena,
  PreviewResult v,
) {
  final out = Uint8List(16);
  final bd = ByteData.sublistView(out);
  bd.setUint32(0, arena.copyIn(v.bytes), Endian.little);
  setInt64LE(bd, 8, v.length);
  return out;
}

PreviewResult _nitroReadStructPreviewResult(NitroWasmModule m, int ptr) {
  final bd = ByteData.sublistView(m.readBytes(ptr, 16));
  final _pbytes = bd.getUint32(0, Endian.little);
  final _nbytes = getInt64LE(bd, 8);
  return PreviewResult(
    bytes: (_pbytes == 0 ? Uint8List(0) : m.readBytes(_pbytes, _nbytes * 1)),
    length: getInt64LE(bd, 8),
  );
}

final class _NitroPrintingWebImpl extends NitroPrinting {
  static final Map<String, _NitroPrintingWebImpl> _instances = {};

  factory _NitroPrintingWebImpl([String key = 'default']) =>
      _instances.putIfAbsent(key, () => _NitroPrintingWebImpl._(key));

  _NitroPrintingWebImpl._(this._key) {
    _m = _module;
    // One lib reference per instance, balancing the releaseLib in
    // dispose() — mirrors the FFI impl taking a loadLib per instance.
    // Without it the first dispose() dropped the count to zero and
    // evicted the module out from under every remaining instance.
    NitroRuntime.retainLib(_libName);
    NitroRuntime.checkAbiVersion(
      _libName,
      () => (_m.call('nitro_printing_nitro_abi_version', const []) as JSNumber)
          .toDartInt,
    );
    NitroRuntime.checkLinkChecksum(
      _libName,
      '4022e793ddcb0252',
      () => _m.readCString(
        dartI64(_m.call('nitro_printing_nitro_bridge_checksum', const [])),
      ),
    );
    _err = WebNitroErrorSlot.alloc(_m);
    final keyArena = WasmArena(_m);
    try {
      _instanceId = dartI64(
        _m.call('nitro_printing_create_instance', [
          keyArena.cString(_key).toJS,
        ]),
      );
    } finally {
      keyArena.releaseAll();
    }
    NitroRuntime.logLifecycle(
      'NitroPrinting',
      'web instance created (key=$_key, id=$_instanceId)',
    );
  }

  final String _key;
  late final NitroWasmModule _m;
  late final WebNitroErrorSlot _err;
  late final int _instanceId;

  // Disposal state lives in HybridObject, NOT in a private field here.
  // A local `_disposed` SHADOWED the base state: dispose() set the copy,
  // `isDisposed` kept reading the base and stayed false forever.
  @override
  void checkDisposed() {
    if (isDisposed) {
      throw StateError(
        'NitroPrinting (web) was disposed — create a new instance',
      );
    }
  }

  @override
  void dispose() {
    if (isDisposed) return;
    _m.call('nitro_printing_destroy_instance', [jsI64(_instanceId)]);
    _err.free();
    _instances.remove(_key);
    NitroRuntime.releaseLib(_libName);
    NitroRuntime.logLifecycle(
      'NitroPrinting',
      'web instance disposed (key=$_key)',
    );
    // Last: flips isDisposed and runs onDestroy(), matching the FFI impl.
    super.dispose();
  }

  // Legacy two-call error protocol — used by @nitroAsync methods, whose C
  // signature carries no NitroError* out-param.
  void _checkLegacyError() {
    final errPtr = dartI64(_m.call('nitro_printing_get_error', const []));
    if (errPtr == 0 || _m.readU8(errPtr) == 0) return;
    final name = _m.readCString(_m.readU32(errPtr + 4));
    final message = _m.readCString(_m.readU32(errPtr + 8));
    final codePtr = _m.readU32(errPtr + 12);
    final stackPtr = _m.readU32(errPtr + 16);
    final code = codePtr != 0 ? _m.readCString(codePtr) : null;
    final stack = stackPtr != 0 ? _m.readCString(stackPtr) : null;
    _m.call('nitro_printing_clear_error', const []);
    throw HybridException(
      name: name,
      message: message,
      code: code,
      stackTrace: stack,
    );
  }

  @override
  bool isPrintingSupported() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_printing_is_printing_supported', [
        jsI64(_instanceId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return (_res as JSNumber).toDartInt != 0;
    }, methodName: 'isPrintingSupported');
  }

  @override
  int getPrintersCount() {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final _res = _m.call('nitro_printing_get_printers_count', [
        jsI64(_instanceId),
        _err.ptr.toJS,
      ]);
      NitroRuntime.throwIfOutParamError(_err);
      return dartI64(_res);
    }, methodName: 'getPrintersCount');
  }

  @override
  String getPrinterDriverVersion(String printerId) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      return withWasmArena(_m, (arena) {
        final _res = _m.call('nitro_printing_get_printer_driver_version', [
          jsI64(_instanceId),
          arena.cString(printerId).toJS,
          _err.ptr.toJS,
        ]);
        NitroRuntime.throwIfOutParamError(_err);
        return _m.readCString(dartI64(_res));
      });
    }, methodName: 'getPrinterDriverVersion');
  }

  @override
  Future<List<PrinterInfo>> getAllPrinters() {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    return NitroRuntime.openNativeAsync<List<PrinterInfo>>(
      call: (dartPort) => _m.call('nitro_printing_get_all_printers', [
        jsI64(_instanceId),
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'getAllPrinters (native-async): native posted null for a non-nullable List<PrinterInfo> result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return RecordReader.decodeIndexedListBytes(
          _framed,
          (r) => PrinterInfoRecordExt.fromReader(r),
        );
      },
      cleanup: _slot.free,
      methodName: 'getAllPrinters',
    );
  }

  @override
  Future<NitroResultValue<PrinterInfo>> getPrinterAt(int index) async {
    checkDisposed();
    return NitroRuntime.callAsync<NitroResultValue<PrinterInfo>>(
      () {
        final _res = _m.call('nitro_printing_get_printer_at', [
          jsI64(_instanceId),
          jsI64(index),
        ]);
        _checkLegacyError();
        final _p = dartI64(_res);
        try {
          final _tag = _m.readBytes(_p, 1)[0];
          if (_tag != 0) {
            return NitroErr<PrinterInfo>(
              RecordReader.fromFramedBytes(_m.readFramed(_p + 1)).readString(),
            );
          }
          return NitroOk(
            PrinterInfoRecordExt.fromReader(
              RecordReader.fromFramedBytes(_m.readFramed(_p + 1)),
            ),
          );
        } finally {
          _m.nitroFree(_p);
        }
      },
      const [],
      methodName: 'getPrinterAt',
    );
  }

  @override
  Future<NitroResultValue<PrinterInfo>> getDefaultPrinter() async {
    checkDisposed();
    return NitroRuntime.callAsync<NitroResultValue<PrinterInfo>>(
      () {
        final _res = _m.call('nitro_printing_get_default_printer', [
          jsI64(_instanceId),
        ]);
        _checkLegacyError();
        final _p = dartI64(_res);
        try {
          final _tag = _m.readBytes(_p, 1)[0];
          if (_tag != 0) {
            return NitroErr<PrinterInfo>(
              RecordReader.fromFramedBytes(_m.readFramed(_p + 1)).readString(),
            );
          }
          return NitroOk(
            PrinterInfoRecordExt.fromReader(
              RecordReader.fromFramedBytes(_m.readFramed(_p + 1)),
            ),
          );
        } finally {
          _m.nitroFree(_p);
        }
      },
      const [],
      methodName: 'getDefaultPrinter',
    );
  }

  @override
  Future<NitroResultValue<PrinterCapabilities>> getPrinterCapabilities(
    String printerId,
  ) async {
    checkDisposed();
    return NitroRuntime.callAsync<NitroResultValue<PrinterCapabilities>>(
      () {
        return withWasmArena(_m, (arena) {
          final _res = _m.call('nitro_printing_get_printer_capabilities', [
            jsI64(_instanceId),
            arena.cString(printerId).toJS,
          ]);
          _checkLegacyError();
          final _p = dartI64(_res);
          try {
            final _tag = _m.readBytes(_p, 1)[0];
            if (_tag != 0) {
              return NitroErr<PrinterCapabilities>(
                RecordReader.fromFramedBytes(
                  _m.readFramed(_p + 1),
                ).readString(),
              );
            }
            return NitroOk(
              PrinterCapabilitiesRecordExt.fromReader(
                RecordReader.fromFramedBytes(_m.readFramed(_p + 1)),
              ),
            );
          } finally {
            _m.nitroFree(_p);
          }
        });
      },
      const [],
      methodName: 'getPrinterCapabilities',
    );
  }

  @override
  Future<PrintResult> printText(String text, {PrintSettings? settings}) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<PrintResult>(
      call: (dartPort) => _m.call('nitro_printing_print_text', [
        jsI64(_instanceId),
        arena.cString(text).toJS,
        (settings == null
                ? 0
                : arena.copyIn(
                    _nitroEncodeFramed((w) => settings.writeFields(w)),
                  ))
            .toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'printText (native-async): native posted null for a non-nullable PrintResult result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return PrintResultRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'printText',
    );
  }

  @override
  Future<PrintResult> printImage(
    Uint8List imageData, {
    PrintSettings? settings,
  }) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<PrintResult>(
      call: (dartPort) => _m.call('nitro_printing_print_image', [
        jsI64(_instanceId),
        arena.copyIn(imageData).toJS,
        imageData.length.toJS,
        (settings == null
                ? 0
                : arena.copyIn(
                    _nitroEncodeFramed((w) => settings.writeFields(w)),
                  ))
            .toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'printImage (native-async): native posted null for a non-nullable PrintResult result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return PrintResultRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'printImage',
    );
  }

  @override
  Future<PrintResult> printPdf(Uint8List pdfData, {PrintSettings? settings}) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<PrintResult>(
      call: (dartPort) => _m.call('nitro_printing_print_pdf', [
        jsI64(_instanceId),
        arena.copyIn(pdfData).toJS,
        pdfData.length.toJS,
        (settings == null
                ? 0
                : arena.copyIn(
                    _nitroEncodeFramed((w) => settings.writeFields(w)),
                  ))
            .toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'printPdf (native-async): native posted null for a non-nullable PrintResult result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return PrintResultRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'printPdf',
    );
  }

  @override
  Future<PrintResult> printDocument(
    PrintDocument document, {
    PrintSettings? settings,
  }) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<PrintResult>(
      call: (dartPort) => _m.call('nitro_printing_print_document', [
        jsI64(_instanceId),
        arena.copyIn(_nitroEncodeFramed((w) => document.writeFields(w))).toJS,
        (settings == null
                ? 0
                : arena.copyIn(
                    _nitroEncodeFramed((w) => settings.writeFields(w)),
                  ))
            .toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'printDocument (native-async): native posted null for a non-nullable PrintResult result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return PrintResultRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'printDocument',
    );
  }

  @override
  Future<bool> printFile(String filePath, {PrintSettings? settings}) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<bool>(
      call: (dartPort) => _m.call('nitro_printing_print_file', [
        jsI64(_instanceId),
        arena.cString(filePath).toJS,
        (settings == null
                ? 0
                : arena.copyIn(
                    _nitroEncodeFramed((w) => settings.writeFields(w)),
                  ))
            .toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return raw is bool ? raw : (raw as num).toInt() != 0;
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'printFile',
    );
  }

  @override
  Future<List<PrintResult>> printBatch(
    List<PrintDocument> documents,
    bool stopOnError, {
    PrintSettings? settings,
  }) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<List<PrintResult>>(
      call: (dartPort) => _m.call('nitro_printing_print_batch', [
        jsI64(_instanceId),
        arena
            .copyIn(
              RecordWriter.encodeIndexedListBytes(
                documents,
                (w, e) => e.writeFields(w),
              ),
            )
            .toJS,
        (stopOnError ? 1 : 0).toJS,
        (settings == null
                ? 0
                : arena.copyIn(
                    _nitroEncodeFramed((w) => settings.writeFields(w)),
                  ))
            .toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'printBatch (native-async): native posted null for a non-nullable List<PrintResult> result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return RecordReader.decodeIndexedListBytes(
          _framed,
          (r) => PrintResultRecordExt.fromReader(r),
        );
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'printBatch',
    );
  }

  @override
  Future<PrintDialogResult> showPrintDialog(
    PrintDocument document, {
    PrintSettings? initialSettings,
  }) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<PrintDialogResult>(
      call: (dartPort) => _m.call('nitro_printing_show_print_dialog', [
        jsI64(_instanceId),
        arena.copyIn(_nitroEncodeFramed((w) => document.writeFields(w))).toJS,
        (initialSettings == null
                ? 0
                : arena.copyIn(
                    _nitroEncodeFramed((w) => initialSettings.writeFields(w)),
                  ))
            .toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'showPrintDialog (native-async): native posted null for a non-nullable PrintDialogResult result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return PrintDialogResultRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'showPrintDialog',
    );
  }

  @override
  Future<PreviewResult> renderPreview(
    PrintDocument document, {
    PrintSettings? settings,
  }) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<PreviewResult>(
      call: (dartPort) => _m.call('nitro_printing_render_preview', [
        jsI64(_instanceId),
        arena.copyIn(_nitroEncodeFramed((w) => document.writeFields(w))).toJS,
        (settings == null
                ? 0
                : arena.copyIn(
                    _nitroEncodeFramed((w) => settings.writeFields(w)),
                  ))
            .toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'renderPreview (native-async): native posted null for a non-nullable PreviewResult result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _v = _nitroReadStructPreviewResult(_m, _ptr);
        _m.nitroFree(_ptr);
        return _v;
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'renderPreview',
    );
  }

  @override
  Future<int> getPageCount(PrintDocument document) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<int>(
      call: (dartPort) => _m.call('nitro_printing_get_page_count', [
        jsI64(_instanceId),
        arena.copyIn(_nitroEncodeFramed((w) => document.writeFields(w))).toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return (raw as num).toInt();
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'getPageCount',
    );
  }

  @override
  Future<bool> printToFile(
    PrintDocument document,
    String outputPath, {
    PrintSettings? settings,
  }) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<bool>(
      call: (dartPort) => _m.call('nitro_printing_print_to_file', [
        jsI64(_instanceId),
        arena.copyIn(_nitroEncodeFramed((w) => document.writeFields(w))).toJS,
        arena.cString(outputPath).toJS,
        (settings == null
                ? 0
                : arena.copyIn(
                    _nitroEncodeFramed((w) => settings.writeFields(w)),
                  ))
            .toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return raw is bool ? raw : (raw as num).toInt() != 0;
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'printToFile',
    );
  }

  @override
  Future<bool> cancelPrintJob(String jobId) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<bool>(
      call: (dartPort) => _m.call('nitro_printing_cancel_print_job', [
        jsI64(_instanceId),
        arena.cString(jobId).toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return raw is bool ? raw : (raw as num).toInt() != 0;
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'cancelPrintJob',
    );
  }

  @override
  Future<bool> pausePrintJob(String jobId) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<bool>(
      call: (dartPort) => _m.call('nitro_printing_pause_print_job', [
        jsI64(_instanceId),
        arena.cString(jobId).toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return raw is bool ? raw : (raw as num).toInt() != 0;
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'pausePrintJob',
    );
  }

  @override
  Future<bool> resumePrintJob(String jobId) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<bool>(
      call: (dartPort) => _m.call('nitro_printing_resume_print_job', [
        jsI64(_instanceId),
        arena.cString(jobId).toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return raw is bool ? raw : (raw as num).toInt() != 0;
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'resumePrintJob',
    );
  }

  @override
  Future<bool> clearPrintQueue() {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    return NitroRuntime.openNativeAsync<bool>(
      call: (dartPort) => _m.call('nitro_printing_clear_print_queue', [
        jsI64(_instanceId),
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return raw is bool ? raw : (raw as num).toInt() != 0;
      },
      cleanup: _slot.free,
      methodName: 'clearPrintQueue',
    );
  }

  @override
  Future<int> getPrintJobsCount() {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    return NitroRuntime.openNativeAsync<int>(
      call: (dartPort) => _m.call('nitro_printing_get_print_jobs_count', [
        jsI64(_instanceId),
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return (raw as num).toInt();
      },
      cleanup: _slot.free,
      methodName: 'getPrintJobsCount',
    );
  }

  @override
  Future<NitroResultValue<PrintJob>> getPrintJobAt(int index) async {
    checkDisposed();
    return NitroRuntime.callAsync<NitroResultValue<PrintJob>>(
      () {
        final _res = _m.call('nitro_printing_get_print_job_at', [
          jsI64(_instanceId),
          jsI64(index),
        ]);
        _checkLegacyError();
        final _p = dartI64(_res);
        try {
          final _tag = _m.readBytes(_p, 1)[0];
          if (_tag != 0) {
            return NitroErr<PrintJob>(
              RecordReader.fromFramedBytes(_m.readFramed(_p + 1)).readString(),
            );
          }
          return NitroOk(
            PrintJobRecordExt.fromReader(
              RecordReader.fromFramedBytes(_m.readFramed(_p + 1)),
            ),
          );
        } finally {
          _m.nitroFree(_p);
        }
      },
      const [],
      methodName: 'getPrintJobAt',
    );
  }

  @override
  Future<NitroResultValue<PrintJob>> getPrintJobStatus(String jobId) async {
    checkDisposed();
    return NitroRuntime.callAsync<NitroResultValue<PrintJob>>(
      () {
        return withWasmArena(_m, (arena) {
          final _res = _m.call('nitro_printing_get_print_job_status', [
            jsI64(_instanceId),
            arena.cString(jobId).toJS,
          ]);
          _checkLegacyError();
          final _p = dartI64(_res);
          try {
            final _tag = _m.readBytes(_p, 1)[0];
            if (_tag != 0) {
              return NitroErr<PrintJob>(
                RecordReader.fromFramedBytes(
                  _m.readFramed(_p + 1),
                ).readString(),
              );
            }
            return NitroOk(
              PrintJobRecordExt.fromReader(
                RecordReader.fromFramedBytes(_m.readFramed(_p + 1)),
              ),
            );
          } finally {
            _m.nitroFree(_p);
          }
        });
      },
      const [],
      methodName: 'getPrintJobStatus',
    );
  }

  @override
  Future<bool> startPrinterDiscovery() {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    return NitroRuntime.openNativeAsync<bool>(
      call: (dartPort) => _m.call('nitro_printing_start_printer_discovery', [
        jsI64(_instanceId),
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return raw is bool ? raw : (raw as num).toInt() != 0;
      },
      cleanup: _slot.free,
      methodName: 'startPrinterDiscovery',
    );
  }

  @override
  Future<bool> stopPrinterDiscovery() {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    return NitroRuntime.openNativeAsync<bool>(
      call: (dartPort) => _m.call('nitro_printing_stop_printer_discovery', [
        jsI64(_instanceId),
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return raw is bool ? raw : (raw as num).toInt() != 0;
      },
      cleanup: _slot.free,
      methodName: 'stopPrinterDiscovery',
    );
  }

  @override
  Future<bool> testPrinterConnection(String printerId, {int? timeoutSeconds}) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<bool>(
      call: (dartPort) => _m.call('nitro_printing_test_printer_connection', [
        jsI64(_instanceId),
        arena.cString(printerId).toJS,
        arena
            .copyIn(const NitroIntWireCodec().encodeBytes(timeoutSeconds))
            .toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return raw is bool ? raw : (raw as num).toInt() != 0;
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'testPrinterConnection',
    );
  }

  @override
  Future<bool> setDefaultPrinter(String printerId) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<bool>(
      call: (dartPort) => _m.call('nitro_printing_set_default_printer', [
        jsI64(_instanceId),
        arena.cString(printerId).toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return raw is bool ? raw : (raw as num).toInt() != 0;
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'setDefaultPrinter',
    );
  }

  @override
  Future<bool> openSystemPrintQueue(String printerId) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<bool>(
      call: (dartPort) => _m.call('nitro_printing_open_system_print_queue', [
        jsI64(_instanceId),
        arena.cString(printerId).toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return raw is bool ? raw : (raw as num).toInt() != 0;
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'openSystemPrintQueue',
    );
  }

  @override
  Future<bool> openPrinterProperties(String printerId) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<bool>(
      call: (dartPort) => _m.call('nitro_printing_open_printer_properties', [
        jsI64(_instanceId),
        arena.cString(printerId).toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return raw is bool ? raw : (raw as num).toInt() != 0;
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'openPrinterProperties',
    );
  }

  @override
  Future<PrintResult> printRaw(Uint8List data, {PrintSettings? settings}) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<PrintResult>(
      call: (dartPort) => _m.call('nitro_printing_print_raw', [
        jsI64(_instanceId),
        arena.copyIn(data).toJS,
        data.length.toJS,
        (settings == null
                ? 0
                : arena.copyIn(
                    _nitroEncodeFramed((w) => settings.writeFields(w)),
                  ))
            .toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'printRaw (native-async): native posted null for a non-nullable PrintResult result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return PrintResultRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'printRaw',
    );
  }

  @override
  Future<PrintResult> printEscPos(
    Uint8List escPosData, {
    PrintSettings? settings,
  }) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<PrintResult>(
      call: (dartPort) => _m.call('nitro_printing_print_esc_pos', [
        jsI64(_instanceId),
        arena.copyIn(escPosData).toJS,
        escPosData.length.toJS,
        (settings == null
                ? 0
                : arena.copyIn(
                    _nitroEncodeFramed((w) => settings.writeFields(w)),
                  ))
            .toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'printEscPos (native-async): native posted null for a non-nullable PrintResult result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return PrintResultRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'printEscPos',
    );
  }

  @override
  Future<PrintResult> printZpl(String zpl, {PrintSettings? settings}) {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    final arena = WasmArena(_m);
    return NitroRuntime.openNativeAsync<PrintResult>(
      call: (dartPort) => _m.call('nitro_printing_print_zpl', [
        jsI64(_instanceId),
        arena.cString(zpl).toJS,
        (settings == null
                ? 0
                : arena.copyIn(
                    _nitroEncodeFramed((w) => settings.writeFields(w)),
                  ))
            .toJS,
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        if (raw == null) {
          throw StateError(
            'printZpl (native-async): native posted null for a non-nullable PrintResult result',
          );
        }
        final _ptr = (raw as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return PrintResultRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      cleanup: () {
        _slot.free();
        arena.releaseAll();
      },
      methodName: 'printZpl',
    );
  }

  @override
  Future<bool> cancelRawPrint() {
    checkDisposed();
    final _slot = WebNitroErrorSlot.alloc(_m);
    return NitroRuntime.openNativeAsync<bool>(
      call: (dartPort) => _m.call('nitro_printing_cancel_raw_print', [
        jsI64(_instanceId),
        _slot.ptr.toJS,
        jsI64(dartPort),
      ]),
      unpack: (raw) {
        _slot.throwIfError();
        return raw is bool ? raw : (raw as num).toInt() != 0;
      },
      cleanup: _slot.free,
      methodName: 'cancelRawPrint',
    );
  }

  @override
  Future<NitroResultValue<PrinterStatusDetail>> getPrinterStatusDetail(
    String printerId, {
    int? timeoutSeconds,
  }) async {
    checkDisposed();
    return NitroRuntime.callAsync<NitroResultValue<PrinterStatusDetail>>(
      () {
        return withWasmArena(_m, (arena) {
          final _res = _m.call('nitro_printing_get_printer_status_detail', [
            jsI64(_instanceId),
            arena.cString(printerId).toJS,
            arena
                .copyIn(const NitroIntWireCodec().encodeBytes(timeoutSeconds))
                .toJS,
          ]);
          _checkLegacyError();
          final _p = dartI64(_res);
          try {
            final _tag = _m.readBytes(_p, 1)[0];
            if (_tag != 0) {
              return NitroErr<PrinterStatusDetail>(
                RecordReader.fromFramedBytes(
                  _m.readFramed(_p + 1),
                ).readString(),
              );
            }
            return NitroOk(
              PrinterStatusDetailRecordExt.fromReader(
                RecordReader.fromFramedBytes(_m.readFramed(_p + 1)),
              ),
            );
          } finally {
            _m.nitroFree(_p);
          }
        });
      },
      const [],
      methodName: 'getPrinterStatusDetail',
    );
  }

  @override
  Stream<PrintJobUpdate> onPrintJobChanged() {
    checkDisposed();
    return NitroRuntime.openStream<PrintJobUpdate>(
      register: (port) => _m.call(
        'nitro_printing_register_on_print_job_changed_stream',
        [jsI64(_instanceId), jsI64(port)],
      ),
      unpack: (message) {
        if (message == null) {
          throw StateError(
            'Received null event on non-nullable stream onPrintJobChanged',
          );
        }
        final _ptr = (message as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return PrintJobUpdateRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      release: (port) => _m.call(
        'nitro_printing_release_on_print_job_changed_stream',
        [jsI64(port)],
      ),
      backpressure: Backpressure.dropLatest,
      debugLabel: 'onPrintJobChanged',
    );
  }

  @override
  Stream<PrinterStatus> onPrinterStatusChanged() {
    checkDisposed();
    return NitroRuntime.openStream<PrinterStatus>(
      register: (port) => _m.call(
        'nitro_printing_register_on_printer_status_changed_stream',
        [jsI64(_instanceId), jsI64(port)],
      ),
      unpack: (message) {
        if (message == null) {
          throw StateError(
            'Received null event on non-nullable stream onPrinterStatusChanged',
          );
        }
        final _ptr = (message as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return PrinterStatusRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      release: (port) => _m.call(
        'nitro_printing_release_on_printer_status_changed_stream',
        [jsI64(port)],
      ),
      backpressure: Backpressure.dropLatest,
      debugLabel: 'onPrinterStatusChanged',
    );
  }

  @override
  Stream<DiscoveredPrinter> onPrinterDiscovered() {
    checkDisposed();
    return NitroRuntime.openStream<DiscoveredPrinter>(
      register: (port) => _m.call(
        'nitro_printing_register_on_printer_discovered_stream',
        [jsI64(_instanceId), jsI64(port)],
      ),
      unpack: (message) {
        if (message == null) {
          throw StateError(
            'Received null event on non-nullable stream onPrinterDiscovered',
          );
        }
        final _ptr = (message as num).toInt();
        final _framed = _m.readFramed(_ptr);
        _m.nitroFree(_ptr);
        return DiscoveredPrinterRecordExt.fromReader(
          RecordReader.fromFramedBytes(_framed),
        );
      },
      release: (port) => _m.call(
        'nitro_printing_release_on_printer_discovered_stream',
        [jsI64(port)],
      ),
      backpressure: Backpressure.dropLatest,
      debugLabel: 'onPrinterDiscovered',
    );
  }
}
