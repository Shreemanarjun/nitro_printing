name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - run: flutter pub get
        working-directory: example
      - run: flutter analyze lib example/lib example/integration_test example/patrol_test

  android:
    name: Android (API 34 emulator)
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
        working-directory: example
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - name: API + transport integration tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          arch: x86_64
          profile: pixel_6
          disable-animations: true
          script: cd example && flutter test integration_test -d emulator-5554
      - name: Patrol suites
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          arch: x86_64
          profile: pixel_6
          disable-animations: true
          script: |
            dart pub global activate patrol_cli
            cd example && patrol test -d emulator-5554

  ios:
    name: iOS (simulator)
    runs-on: macos-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
        working-directory: example
      - name: Boot simulator
        id: sim
        run: |
          UDID=$(xcrun simctl list devices available --json | python3 -c "import json,sys; devs=[d for r in json.load(sys.stdin)['devices'].values() for d in r if 'iPhone' in d['name']]; print(devs[-1]['udid'])")
          xcrun simctl boot "$UDID"
          xcrun simctl bootstatus "$UDID" -b
          echo "udid=$UDID" >> "$GITHUB_OUTPUT"
      - name: API + transport integration tests
        run: flutter test integration_test -d ${{ steps.sim.outputs.udid }}
        working-directory: example
      - name: Patrol suites
        run: |
          dart pub global activate patrol_cli
          (cd ios && pod install)
          patrol test -d ${{ steps.sim.outputs.udid }}
        working-directory: example

  macos:
    name: macOS (desktop)
    runs-on: macos-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter config --enable-macos-desktop
      - run: flutter pub get
        working-directory: example
      # Desktop embedding can fail to relaunch the app between test files in a
      # single `flutter test integration_test` invocation — run files separately.
      - name: API integration tests
        run: flutter test integration_test/nitro_printing_test.dart -d macos
        working-directory: example
      - name: Native transport tests
        run: flutter test integration_test/native_transport_test.dart -d macos
        working-directory: example

  linux:
    name: Linux (desktop)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    # Non-blocking: this is the first real Linux desktop validation. The plugin
    # C++ backend compiles against the 0.5.10 bridge, and the example desktop
    # wiring was fixed as a stopgap (portable NITRO_NATIVE + removed the
    # undefined-${PLUGIN_NAME} block that nitrogen link injects into the runner
    # CMakeLists). Flip to blocking once a Linux build is confirmed green here.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install desktop dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build libgtk-3-dev xvfb
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter config --enable-linux-desktop
      - run: flutter pub get
        working-directory: example
      - name: Build
        run: flutter build linux --debug
        working-directory: example
      - name: API integration tests (headless X)
        run: xvfb-run -a flutter test integration_test/nitro_printing_test.dart -d linux
        working-directory: example
      - name: Native transport tests (headless X)
        run: xvfb-run -a flutter test integration_test/native_transport_test.dart -d linux
        working-directory: example

  windows:
    name: Windows (desktop)
    runs-on: windows-latest
    timeout-minutes: 45
    # Non-blocking: first real Windows desktop validation (see the linux job).
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter config --enable-windows-desktop
      - run: flutter pub get
        working-directory: example
      - name: Build
        run: flutter build windows --debug
        working-directory: example
      - name: API integration tests
        run: flutter test integration_test/nitro_printing_test.dart -d windows
        working-directory: example
      - name: Native transport tests
        run: flutter test integration_test/native_transport_test.dart -d windows
        working-directory: example
